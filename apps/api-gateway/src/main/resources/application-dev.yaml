# Development profile for api-gateway
# Activate with: SPRING_PROFILES_ACTIVE=dev

# JWT configuration for development
jwt:
  enabled: false  # Disable JWT validation in dev mode for easier testing

logging:
  level:
    org.jarvis.apigateway: DEBUG
    org.jarvis.apigateway.filter: DEBUG

