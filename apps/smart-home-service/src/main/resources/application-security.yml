security:
  # Device action whitelist
  allowed-actions:
    - TURN_ON
    - TURN_OFF
    - DIM
    - BRIGHTEN
    - SET_COLOR
    - SET_TEMPERATURE
    - LOCK
    - UNLOCK
  
  # Blocked for safety:
  # - DISABLE_ALARM
  # - UNLOCK_ALL
  # - OPEN_GARAGE (without explicit confirmation)
