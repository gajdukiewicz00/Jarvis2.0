# Legacy Documentation

This folder contains historical notes and iteration logs. These documents may reference
deprecated workflows (minikube, docker-compose, NodePort) and are kept only for history.

Current, supported flow:
- `./jarvis-launch.sh`
- k3s + ingress-nginx + HTTPS
- local secrets in `~/.jarvis/secrets`
