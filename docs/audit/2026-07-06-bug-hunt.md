# Jarvis2.0 — Multi-Agent Bug Hunt (2026-07-06)

19 language-specific reviewer agents swept every module; each finding was adversarially re-verified by a second agent whose default stance was to refute it. 77 agents total, 0 errors.

**Result: 56 CONFIRMED bugs** (raw 58, refuted 2, uncertain 0).

Severity: CRITICAL 12 · HIGH 24 · MEDIUM 19 · LOW 1

## 1. [CRITICAL] security-service — auth-bypass

**Location:** `apps/security-service/src/main/java/org/jarvis/security/service/AuthService.java:361`

**Bug:** AuthService.revokeAllRefreshTokens() only revokes refresh tokens in the DB and never bumps User.tokensValidFrom, so an already-issued access token stays valid after password change or account disablement.

**Failure scenario:** A user's access token is stolen, and the legitimate user changes their password via POST /auth/password/change (AuthService.changePassword, line 320 calls revokeAllRefreshTokens(user.getId(), REVOKE_REASON_PASSWORD_CHANGED)) expecting all prior sessions to be killed. Only the refresh_tokens rows are marked revoked; the private revokeAllRefreshTokens() method (lines 361-364) never sets user.setTokensValidFrom(now) the way TokenRevocationService.revokeAllForUser() does (TokenRevocationService.java:97-99). Because AuthService.getUserFromToken()'s isBeforeSessionFloor() check (AuthService.java:269-284) is the ONLY server-side mechanism that can reject an already-minted access token, and the floor was never moved, the stolen access token continues to authenticate successfully against /auth/me, /auth/password/change, etc. for the remainder of its TTL (up to accessExpirationMs) even though the account owner believes the session was terminated. The identical bug also fires when refresh() disables an account (line 162: revokeAllRefreshTokens(user.getId(), REVOKE_REASON_ACCOUNT_DISABLED)) — a disabled user's already-issued access token keeps working until natural expiry instead of being cut off immediately.

**Fix:** In AuthService.revokeAllRefreshTokens (line 361), after revoking refresh tokens, also load the user and bump the session floor: userRepository.findById(userId).ifPresent(u -> { u.setTokensValidFrom(now); userRepository.save(u); }) — mirroring TokenRevocationService.revokeAllForUser lines 97-100. Better: delegate AuthService's four call sites (lines 162, 178, 197, 320) to inject and call TokenRevocationService.revokeAllForUser directly, removing the duplicated/divergent private method entirely so there is a single source of truth for "revoke all sessions."

<sub>Verifier: AuthService.revokeAllRefreshTokens (lines 361-364) only calls refreshTokenRepository.revokeAllActiveTokensForUser(...) and updates SecurityMetrics — it never touches user.tokensValidFrom, even though AuthService already holds a UserRepository field (line 35) it could use to do so. Contrast with TokenRevocationService.revokeAllForUser (lines 89-107), which does the refresh-token revocation AND explicitly sets user.setTokensValidFrom(now) + userRepository.save(user) (lines 97-100), with a doc comment stating this is exactly what makes revoke-all effective against already-issued access tokens.

g</sub>

## 2. [CRITICAL] jarvis-common — pii-logging

**Location:** `apps/jarvis-common/src/main/java/org/jarvis/common/logging/LogSanitizer.java:35`

**Bug:** LogSanitizer's PII-protection boolean is inverted: when the shared `logging.pii.enabled` flag is false (the actual default shipped in memory-service and llm-service application.yml, `${LOG_PII_ENABLED:false}`), sanitizeId()/sanitizeText() return the RAW, unredacted value instead of a hash.

**Failure scenario:** In memory-service/llm-service, `logging.pii.enabled` defaults to false (see apps/memory-service/src/main/resources/application.yml:134 and apps/llm-service/src/main/resources/application.yml:178, both `enabled: ${LOG_PII_ENABLED:false}`). LogSanitizer is constructed with that same boolean as `piiEnabled` (e.g. MemoryController.java:194). In sanitizeId (line 35: `if (!piiEnabled) return id;`) and sanitizeText (line 49: `if (!piiEnabled) return text;`), piiEnabled=false causes the RAW id/text to be returned unmodified — the hash-only path only triggers when piiEnabled=true. So with the shipped default (false), every call like `log.info("... userId={}", sanitizer.sanitizeId(userId))` in MemoryController (lines 61-62, 95-96, 123-124, 149-150) logs the actual user ID, session ID, and full free-text query in plaintext, in every environment including production. This is the opposite of the class's own documented intent ("Returns a short stable hash in PII-safe mode") and the opposite of PiiLoggingGuard's assumption (PiiLoggingGuard.java only fails fast when this same flag is TRUE in prod, treating false as the safe default) — so the fail-fast guard never catches this misconfiguration because it is checking the wrong polarity relative to what LogSanitizer actually does. Net effect: PII (user/session IDs and raw query text) is written to logs by default, undetected by the safety guard meant to prevent exactly this.

**Fix:** Align polarity across the codebase: either (a) invert LogSanitizer's condition to `if (piiEnabled) return id;`/`return text;` and `else return shortHash(...)` so `logging.pii.enabled=true` means \"raw PII logging permitted\" (matching PiiLoggingGuard's semantics and the property name), or (b) invert PiiLoggingGuard to fail when `!piiLoggingEnabled` is not what's intended — but (a) is more correct given the property name `logging.pii.enabled`. After fixing, update LogSanitizerTest.java accordingly, and change the shipped defaults in application.yml (`LOG_PII_ENABLED:false`) to verify sanitizeId/sanitizeText actually redact by default in memory-service and llm-service. Add an integration test asserting that with the shipped default env (no LOG_PII_ENABLED set), sanitizeId/sanitizeText do NOT return raw input.

<sub>Verifier: Verified line-by-line against the actual source:

1. LogSanitizer.java lines 31-39: `sanitizeId` returns the raw `id` unmodified when `!piiEnabled` (line 35-37), and only hashes when `piiEnabled` is true. Same pattern in `sanitizeText` (lines 49-50). This is confirmed by LogSanitizer's own unit tests (LogSanitizerTest.java): `sanitizeIdReturnsShortHashWhenPiiEnabled` (constructs with `true` → hash) and `sanitizerCanBeDisabledForLocalDebug` (constructs with `false` → raw values returned unchanged). So the class's own behavior/tests confirm: piiEnabled=true → safe/hashed, piiEnabled=false → raw/</sub>

## 3. [CRITICAL] api-gateway — authz-bypass

**Location:** `apps/api-gateway/src/main/java/org/jarvis/apigateway/filter/PanicGuardFilter.java:31`

**Bug:** The global panic kill-switch's BLOCKED_PREFIXES allowlist omits the PC desktop action route and the generic PC-control / agent-swarm proxies, so engaging panic does not actually stop real-world actions through those routes.

**Failure scenario:** An operator (or automated safety trigger) calls POST /api/v1/agent/panic to engage SystemPanicState, expecting 'All actions are halted by the global kill switch' (per the filter's own error message and class javadoc: 'refuse every action-bearing request'). However BLOCKED_PREFIXES only contains '/api/v1/agent/execute', '/api/v1/orchestrator/voice', and '/api/v1/tools'. A caller can still POST to '/api/v1/pc/desktop/action' (PcDesktopController — the official OPEN_APP/TYPE_TEXT/HOTKEY/SCREENSHOT executor, which never checks SystemPanicState itself), or to the generic '/api/v1/pc/**' passthrough (PcControlProxyController), or to '/api/v1/agents/swarm' / '/api/v1/agents/tasks/**' (AgentServiceProxyController, which triggers real SwarmCoordinator execution per ProxyTimeoutPolicy's own comment) — none of these prefixes match, so requests sail through with a 200 while the caller believes the kill switch is active.

**Fix:** Expand BLOCKED_PREFIXES to include "/api/v1/pc" (covers both /api/v1/pc/desktop/action and the generic proxy since both share that prefix) and "/api/v1/agents" (plural, covers swarm/tasks proxy), or better, invert the logic to an explicit allowlist of read-only/panic-control paths that stay open (e.g. GET requests, /api/v1/agent/panic*, /api/v1/agent/register, /api/v1/agent/heartbeat) and block every other mutating request by default so newly added action routes are safe-by-default instead of requiring a filter update each time.

<sub>Verifier: PanicGuardFilter.BLOCKED_PREFIXES (line 31-34) only contains "/api/v1/agent/execute", "/api/v1/orchestrator/voice", "/api/v1/tools", and isActionPath() (line 60) uses uri::startsWith against exactly those three strings. Verified via reading the three controllers named in the report:
- PcDesktopController (@RequestMapping("/api/v1/pc/desktop")) exposes POST /api/v1/pc/desktop/action, the real allowlisted desktop-action executor forwarding to pc-control via PcControlClient. It never references SystemPanicState.
- PcControlProxyController (@RequestMapping("/api/v1/pc")) is a generic wildcard prox</sub>

## 4. [CRITICAL] api-gateway — auth-bypass

**Location:** `apps/api-gateway/src/main/java/org/jarvis/apigateway/agent/AgentControlController.java:43`

**Bug:** No role/authority check guards the global panic engage/clear endpoints or agent registration/kill-switch endpoints — any authenticated user (plain JWT with role USER) can trigger or clear the system-wide safety kill switch.

**Failure scenario:** SecurityConfig only requires 'anyRequest().authenticated()' for this path (no admin-only requestMatcher, and AgentControlController has no @PreAuthorize). A regular logged-in user calls POST /api/v1/agent/panic to engage a global panic for every user of the platform (DoS), or POST /api/v1/agent/panic/clear to silently clear a panic that an admin/security process engaged for a legitimate safety reason, or POST /api/v1/agent/{agentId}/kill-switch to disengage another user's/agent's kill switch — none of these require an ADMIN/OPERATOR authority, only a valid access token.

**Fix:** Add @PreAuthorize("hasAuthority('ADMIN')") (or a dedicated SAFETY_OPERATOR authority) to engagePanic, clearPanic, and killSwitch methods, and/or add explicit requestMatchers in SecurityConfig restricting POST /api/v1/agent/panic, /api/v1/agent/panic/clear, and /api/v1/agent/*/kill-switch to an admin/operator role, consistent with the existing @PreAuthorize("hasAuthority('SVC_INTERNAL')") pattern used in PcControlInternalController.

<sub>Verifier: AgentControlController.java lines 43-92 defines POST /api/v1/agent/panic, /api/v1/agent/panic/clear, and /api/v1/agent/{agentId}/kill-switch with no @PreAuthorize, @Secured, or any role/authority check. SecurityConfig.java (line 52) only enforces `.anyRequest().authenticated()` for all non-public paths, including this controller's package — there is no admin-specific requestMatcher for /api/v1/agent/**. @EnableMethodSecurity is active on the app (SecurityConfig line 27), so @PreAuthorize would work if applied, but it simply isn't used here. Confirming the pattern elsewhere in the same package </sub>

## 5. [CRITICAL] smart-home-service — auth-bypass

**Location:** `apps/smart-home-service/src/main/java/org/jarvis/smarthome/controller/SmartHomeController.java:205`

**Bug:** GET /devices/{deviceId}/state-history does not take or check a user/tenant identity and DeviceStateHistoryRepository#findByDeviceIdOrderByRecordedAtDesc filters only by deviceId, so it returns every user's action history (including action payloads and full device state snapshots) for a shared deviceId, breaking the per-user isolation enforced everywhere else in this service (StatefulSmartHomeService.getOrCreateState keys state by "userId::deviceId").

**Failure scenario:** User A executes LOCK/UNLOCK or SET_TEMPERATURE actions on front_door_lock (their own per-user state, isolated via stateStore key "userA::front_door_lock"). User B calls GET /api/v1/smarthome/devices/front_door_lock/state-history (no X-User-Id needed, no ownership check) and receives User A's full history rows — userId, action, payload, and stateJson (e.g. lock state or thermostat set-point) — for the same physical/logical deviceId, since DeviceStateHistoryRepository.java line 10 has no userId predicate and DeviceStateHistoryService.history(deviceId, limit) (line 46-48) never receives or applies one.

**Fix:** Require X-User-Id on this endpoint (call requireUserId as the other endpoints do), add a userId column predicate to the repository query (e.g. findByUserIdAndDeviceIdOrderByRecordedAtDesc(userId, deviceId, pageable)), and pass userId through DeviceStateHistoryService.history(userId, deviceId, limit) so results are scoped to the caller's own history rows only.

<sub>Verifier: SmartHomeController.deviceStateHistory (lines 205-210) is the only data-returning endpoint in this controller that does not accept/require an X-User-Id header or call requireUserId(...); it forwards only deviceId and limit to DeviceStateHistoryService.history(deviceId, limit) (history.DeviceStateHistoryService lines 46-48), which calls DeviceStateHistoryRepository.findByDeviceIdOrderByRecordedAtDesc(deviceId, pageable) (DeviceStateHistoryRepository.java line 10) — a query with no userId predicate whatsoever. Meanwhile DeviceStateHistoryEntry (history/DeviceStateHistoryEntry.java) persists a us</sub>

## 6. [CRITICAL] pc-user-cloud — auth-bypass

**Location:** `apps/pc-control/src/main/java/org/jarvis/pccontrol/filter/TokenValidationFilter.java:31`

**Bug:** The admin-only authorization gate protects paths that don't exist, so every real PC action (dispatched through the single generic action endpoint) runs with zero role check.

**Failure scenario:** ADMIN_ONLY_PATHS only lists "/api/v1/pc/shutdown" and "/api/v1/pc/restart" (lines 31-33), and isAdminOnlyPath()/hasAdminRole() (lines 60-66, 82-88) only gate those two literal paths. Neither endpoint is ever mapped anywhere in the codebase (grep across pc-control controllers finds no such route) — all actions, including sensitive ones like SYSTEM_COMMAND sleep/monitor_off, LOCK_SCREEN, EMPTY_TRASH, OPEN_APP, TYPE_TEXT, etc., are dispatched through the single POST /api/v1/pc/action endpoint (PcControlController.executeAction -> DefaultPcActionExecutionService.execute), which is never in ADMIN_ONLY_PATHS. CommandValidator only checks the action type against a static allow-list, never against the caller's role. Consequently any authenticated non-admin user (any request with a non-empty X-User-Id header and no ADMIN role) can call POST /api/v1/pc/action with actionType=SYSTEM_COMMAND, parameters={command: sleep} (or monitor_off, lock-screen, empty-trash, etc.) and it executes successfully — the admin gate the code appears to implement is unreachable dead code.

**Fix:** Perform role-based authorization based on the actual action being requested rather than the URL path — e.g. have TokenValidationFilter (or a filter/interceptor downstream of body parsing) classify PcActionRequest.actionType() and require ADMIN role for a defined set of privileged action types (SYSTEM_COMMAND, LOCK_SCREEN, etc.), or move the role check into DefaultPcActionExecutionService/CommandValidator where the actionType is already available, replacing/removing the now-dead ADMIN_ONLY_PATHS path-based check.

<sub>Verifier: TokenValidationFilter.ADMIN_ONLY_PATHS (lines 31-33) only contains "/api/v1/pc/shutdown" and "/api/v1/pc/restart", and isAdminOnlyPath()/hasAdminRole() (lines 60-66, 82-88) gate solely on those literal path prefixes. A full controller-mapping scan of apps/pc-control (PcControlController, DesktopControlController, DesktopCapabilitiesController, FileController) shows no route for either path exists anywhere in the app — the only "shutdown" hit in the whole module is Spring Boot's unrelated `server.shutdown: graceful` config property. Meanwhile every real PC action (SYSTEM_COMMAND sleep/monitor_o</sub>

## 7. [CRITICAL] vision-security — path-traversal

**Location:** `apps/vision-security-service/src/main/java/org/jarvis/visionsecurity/controller/CvAnalysisController.java:43`

**Bug:** POST /api/v1/vision-security/cv/analyze (source=screenshot) takes the client-supplied `imagePath` and passes it unsanitized as the screenshot destination all the way to ScreenshotService.capture(), which does Files.createDirectories(target.getParent()) and then writes/overwrites a PNG at that exact path.

**Failure scenario:** Any authenticated caller posts {"source":"screenshot","imagePath":"/home/<user>/.bashrc"} (or any other path the service process can write, e.g. a cron file or another user's incident directory). The service silently overwrites that file with screenshot bytes — no whitelist/canonicalization against the configured storage root exists anywhere in the call chain (CvAnalysisController -> LocalCvService.analyzeScreenshot -> ScreenshotService.capture).

**Fix:** In CvAnalysisController (or better, centrally in LocalCvService/ScreenshotService), resolve the client-supplied imagePath against the configured storage root (properties.getStorage().getRoot()) using target.toAbsolutePath().normalize(), then verify it starts with the canonicalized root path before use; reject with 400 if it escapes the root. Apply the same containment check to analyzeFile(), screen-context, and ask-screen imagePath inputs, and add Bean Validation (e.g. a custom @ValidStoragePath constraint) on the AnalyzeRequest/ScreenContextRequest/AskScreenRequest records so invalid paths are rejected before reaching the service layer.

<sub>Verifier: CvAnalysisController.analyze() (lines 38-52) takes request.imagePath() from the request body verbatim and converts it with Path.of(request.imagePath()) with zero validation, canonicalization, or whitelisting against any configured storage root. This `target` is passed straight into LocalCvService.analyzeScreenshot(Path destination), which (LocalCvService.java lines 84-91) uses `destination` as-is if non-null (only falling back to defaultScreenshotPath() when destination == null) and calls screenshotService.capture(target). ScreenshotService.capture() (lines 42-65) immediately does `Files.creat</sub>

## 8. [CRITICAL] vision-security — path-traversal

**Location:** `apps/vision-security-service/src/main/java/org/jarvis/visionsecurity/controller/CvAnalysisController.java:51`

**Bug:** POST /api/v1/vision-security/cv/analyze (source=file, default) takes client-supplied `imagePath` and passes it directly to LocalCvService.analyzeFile()/OCR engine with no containment check against the per-user storage root used by EnrollmentStore/IncidentStore.

**Failure scenario:** Any authenticated user can call analyze with imagePath pointing at another user's stored evidence (e.g. .../users/<other-user>/incidents/<uuid>/screenshot.png or enrollment sample PNGs) and receive the OCR'd text back in the response — completely bypassing the userId-scoped isolation that EnrollmentStore/IncidentStore otherwise enforce (IDOR). It equally allows probing/reading any other locally readable image file on the host.

**Fix:** Require the analyze endpoint to accept an authenticated userId + logical reference (e.g. incidentId/enrollmentId) resolved server-side via IncidentStore/EnrollmentStore rather than a raw filesystem path; if a raw path must be supported, add `authentication` to the controller method, resolve `imagePath.toRealPath()` and verify it is contained within that user's storage root (`storage.root/users/<sanitize(userId)>/...`) via `startsWith`, rejecting with 403/400 otherwise.

<sub>Verifier: CvAnalysisController.analyze() (line 51, source=file branch) takes request.imagePath() straight from the client JSON body and does `Path.of(request.imagePath())` with zero validation, then calls `cvService.analyzeFile(...)`. Tracing into LocalCvService.analyzeFile -> analyze(): the only checks are `imagePath == null` and `Files.isRegularFile(imagePath)` (LocalCvService.java lines 113-126) — there is no canonicalization, no prefix/containment check against any per-user root, and no comparison against the authenticated principal at all. In fact this endpoint doesn't even receive an `Authenticati</sub>

## 9. [CRITICAL] desktop-javafx — path-traversal

**Location:** `apps/desktop-javafx/src/main/kotlin/org/jarvis/agent/command/DefaultDesktopActions.kt:138`

**Bug:** CREATE_LOCAL_NOTE's `directory` parameter is taken straight from the inbound RabbitMQ command payload (via NativeDesktopCommandExecutor.kt:98, `stringField(payload, "directory")?.let(Path::of)`) and used unchecked as the write target, with no canonicalization or containment check against the intended `~/.jarvis/notes` root.

**Failure scenario:** A CommandEnvelope with intent=CREATE_LOCAL_NOTE and payload `{"title":"x","body":"...","directory":"/home/user/.config/autostart"}` (or any writable absolute path) is delivered on `jarvis.commands.agent.execute`. `PermissionAwareExecutor` only checks the kill switch (PermissionGate.ensureClearOnly()) before delegating — there is no per-intent capability or path allowlist — so `Files.createDirectories(target)` and `Files.write(...)` execute against the attacker-chosen directory, creating a file with attacker-controlled title/body content anywhere the process user can write (e.g. planting an autostart entry or overwriting a config file), defeating the documented 'write a markdown note under ~/.jarvis/notes' guarantee.

**Fix:** In createLocalNote, resolve and canonicalize the target against notesRoot and reject if it escapes: e.g. `val target = (directory ?: notesRoot).let { notesRoot.resolve(it) }.normalize(); require(target.startsWith(notesRoot)) { "directory escapes notes root" }` — or simpler, ignore/reject any caller-supplied `directory` that is not a relative subpath of notesRoot, and add a capability check in PermissionAwareExecutor/PermissionGate for filesystem-writing intents (CREATE_LOCAL_NOTE) analogous to the OPEN_APP allowlist.

<sub>Verifier: DefaultDesktopActions.createLocalNote (line 138): `val target = (directory ?: notesRoot).toAbsolutePath()` — the attacker-supplied `directory` Path, if non-null, is used verbatim with no normalization, no `startsWith(notesRoot)` containment check, and no canonical-path comparison. It's then used directly for `Files.createDirectories(target)` and `Files.write(file, ...)` (lines 140-150).

Tracing the call chain confirms every step of the reported scenario:
1. NativeDesktopCommandExecutor.kt:98 — `directory = stringField(payload, "directory")?.let(Path::of)` takes the raw JSON payload string fro</sub>

## 10. [CRITICAL] android-app — auth-bypass

**Location:** `apps/android-app/app/src/main/java/org/jarvis/android/notifications/OtpGuard.kt:20`

**Bug:** OtpGuard's keyword list has no pattern for BLIK, Poland's dominant one-time-code mobile payment/authorization mechanism, even though PKO Bank Polski (IKO) — the leading BLIK issuer — is in BankAppRegistry's default whitelist.

**Failure scenario:** PKO/IKO (or any Polish bank) posts a real notification like 'Kod BLIK do autoryzacji transakcji: 483920' or 'Twój kod BLIK: 483920'. None of the English patterns match ('otp', 'verification code', 'authorization code', '2fa', etc.) and the Polish pattern requires 'kod' to be immediately followed by autoryzacyjny|weryfikacyjny|jednorazowy|sms|dostępu|logowania|potwierdzaj*, none of which is 'BLIK'. isOtpOrAuthCode() returns false, so handlePosted() does not drop it. Because the numeric code is only 6 digits (below NotificationSanitizer's 9-digit masking threshold), the plaintext BLIK code is written unmasked into the FINANCE_ENTRY draft and queued for sync — exactly the account-takeover scenario the module's own docstring says must never happen.

**Fix:** Add a BLIK-specific pattern to KEYWORD_PATTERNS, e.g. \bblik\b (case-insensitive, matches standalone or as part of "kod BLIK"/"BLIK code"), and broaden the Polish "kod ..." pattern to also match when an authorization-related word appears anywhere in the phrase rather than requiring it immediately after "kod" (e.g. split into two checks: kod\s+blik and a broader authoriz(?:acj|uj)\w* check), plus add test cases mirroring real PKO/IKO BLIK notification copy.

<sub>Verifier: OtpGuard.KEYWORD_PATTERNS (lines 20-38) has no BLIK-related pattern in either the English or Polish sections. A realistic PKO/IKO BLIK notification like "Kod BLIK do autoryzacji transakcji: 483920" does not match any pattern: the Polish rule at line 36, `kod (?:autoryzacyjny|weryfikacyjny|jednorazowy|sms|dostępu|logowania|potwierdzaj\w*)`, requires the word immediately following "kod" to be one of those specific words — but here "kod" is immediately followed by "BLIK", not "autoryzacyjny" etc., so it fails to match even though "autoryzacji" appears later in the same sentence. None of the Engli</sub>

## 11. [CRITICAL] android-app — auth-bypass

**Location:** `apps/android-app/app/src/main/java/org/jarvis/android/sync/Pairing.kt:60`

**Bug:** The pairing handshake authenticates the device to the server (Ed25519 signature) but never authenticates the server to the device — deriveSessionKeyFromResponse() unconditionally trusts resp.serverKexPubB64 with no signature/MAC to verify it actually came from the paired sync-service, and the manifest sets android:usesCleartextTraffic="true" (AndroidManifest.xml:18) for the exact on-prem/LAN traffic this protocol runs over.

**Failure scenario:** An attacker on the same LAN (or a rogue AP, or anyone able to intercept the plaintext HTTP POST to /pairing/complete since cleartext is permitted) responds to pairingComplete() with their own X25519 public key instead of the real server's. deriveSessionKeyFromResponse() computes an ECDH shared secret with the attacker's key and derives a session key the attacker also possesses — with no way for the client to detect the substitution. Every subsequent SyncWorker upload (encrypted with that session key) is then transparently decryptable by the attacker, and PairingState persists the attacker-controlled routingId/session forever until the user re-pairs, silently compromising all finance/health sync data.

**Fix:** Have the server sign (pairingNonceB64 || serverKexPubB64) with a long-lived server Ed25519 identity key that ships with the app (or is pinned on first pair, TOFU-style, and persisted in PairingState for comparison on re-pair); verify that signature in deriveSessionKeyFromResponse() before deriving the session key, reject the response otherwise. Additionally scope android:usesCleartextTraffic via a network_security_config.xml restricted to known on-prem hosts/IP ranges (or require HTTPS with certificate pinning via OkHttp CertificatePinner) rather than allowing cleartext globally.

<sub>Verifier: Pairing.kt implements a one-way-authenticated handshake: the device signs (nonce || deviceKexPub) with its Ed25519 identity key (Pairing.kt:38-39), which lets the server authenticate the device. But deriveSessionKeyFromResponse() (Pairing.kt:55-64) takes resp.serverKexPubB64 straight off the wire, decodes it (line 60), and feeds it directly into x25519Agree()/deriveSessionKey() with zero verification — no signature field on the server's public key exists in SyncClient.PairingResponse (SyncClient.kt:39-45), no certificate/identity pinning, and PairingState.kt never persists a server identity ke</sub>

## 12. [CRITICAL] python-services — concurrency

**Location:** `apps/llm-server-py/app/main.py:297`

**Bug:** Streaming chat handler iterates the model's blocking token generator directly inside an async function, with no thread/executor offload, freezing the asyncio event loop for the whole response duration.

**Failure scenario:** A client sends a chat request with stream=true. Inside generate_stream() (main.py:270-310), `for token in stream:` at line 297 repeatedly calls next() on a synchronous llama.cpp/transformers generator without awaiting or dispatching to the ThreadPoolExecutor (unlike the non-streaming path at lines 219-233 which uses loop.run_in_executor). Because this runs on the single asyncio event loop thread, every other concurrent request — including GET /health used by k8s liveness/readiness probes — is blocked until the entire streamed generation finishes. Under any real generation latency (seconds to tens of seconds), the whole server appears hung to all other clients and can be killed by liveness probes.

**Fix:** Wrap the blocking generator consumption using an executor-backed bridge, e.g. run the underlying sync generator in a thread and push tokens through an asyncio.Queue (or use anyio.to_thread.run_sync per next() call, or asyncio.to_thread on a helper that drains the generator into a queue), so each token retrieval yields control back to the event loop; also add a timeout equivalent to MAX_GENERATION_SECONDS for the streaming path.

<sub>Verifier: main.py's generate_stream() (async def, lines 270-310) does `for token in stream:` at line 297 where `stream` is a plain synchronous generator returned by model_loader.chat_stream()/generate_stream(), which in turn yield from the backend implementations. For llama.cpp (backends/llamacpp_backend.py:242-305) each next() call performs a blocking synchronous inference call into the C library with no await point. For transformers (backends/transformers_backend.py:178-233) generation happens on a background Thread, but the consuming `for text in streamer:` loop blocks synchronously on the streamer's</sub>

## 13. [HIGH] security-service — race

**Location:** `apps/security-service/src/main/java/org/jarvis/security/service/AuthService.java:172`

**Bug:** Refresh-token rotation in AuthService.refresh() is a non-atomic check-then-act (read storedToken, check revokedAt==null, then save) with no optimistic-lock/version column or atomic conditional update, unlike the bulk revoke query.

**Failure scenario:** Two concurrent POST /auth/refresh requests present the same still-valid refresh token (e.g. a network retry, a compromised token being replayed by an attacker at the same time as the legitimate client). Both transactions read the same RefreshToken row (line 172) before either commits, both see getRevokedAt()==null and isActive(now)==true (lines 176-189), and both proceed to mint a fresh replacement refresh token and save the original as revoked (lines 204-211). Because RefreshToken (model/RefreshToken.java) has no @Version field and the update path is a plain JPA save rather than the atomic conditional UPDATE used by revokeAllActiveTokensForUser (RefreshTokenRepository.java:21-32), both requests can succeed, producing two live child tokens from one rotation and silently defeating the refresh-token-reuse detection (REVOKE_REASON_REUSE) that the rest of the code relies on to detect stolen-token replay.

**Fix:** Add a @Version column to RefreshToken (optimistic locking) so the concurrent save() throws OptimisticLockException on the loser, or replace the read-then-save with an atomic conditional @Modifying update (e.g. `UPDATE RefreshToken SET revokedAt=:now, replacedByTokenId=:newId, revokeReason=:reason WHERE tokenId=:id AND revokedAt IS NULL`) and treat affectedRows==0 as a concurrent-rotation/reuse condition (revoke all sessions), mirroring the pattern already used by revokeAllActiveTokensForUser.

<sub>Verifier: AuthService.refresh() (lines 172-211) does a classic non-atomic check-then-act on the RefreshToken row: it loads the row with a plain findById (RefreshTokenRepository extends JpaRepository<RefreshToken, UUID>, so this is an unlocked SELECT), checks storedToken.getRevokedAt() == null and storedToken.isActive(now) in Java, then later calls storedToken.setRevokedAt(now)/setReplacedByTokenId(...)/save(storedToken).

Verified RefreshToken.java has no @Version field (no optimistic-locking column at all), so JPA/Hibernate's dirty-checking UPDATE is an unconditional `UPDATE ... SET revoked_at=?, repla</sub>

## 14. [HIGH] memory-service — logic-error

**Location:** `apps/memory-service/src/main/java/org/jarvis/memory/obsidian/MemoryNoteRepository.java:35`

**Bug:** searchByText's "exclude deleted" filter compares status against the lowercase literal 'deleted', but MemoryForgetService persists the status as uppercase 'DELETED', so the exclusion never actually filters anything.

**Failure scenario:** User says "Jarvis, forget this" on a note titled "Mom's surgery date"; MemoryForgetService.forget() sets status="DELETED" and clears body/summary but leaves title intact (MemoryForgetService.java:88-93). A later keyword search (MemoryNoteService.searchUnified fallback, or MemoryForgetService.resolveMatches for forget-by-query) still calls repository.searchByText(query, pageable), whose JPQL is `WHERE (n.status IS NULL OR n.status <> 'deleted') AND (title LIKE ... OR body LIKE ...)`. Because 'DELETED' <> 'deleted' evaluates true in Postgres (case-sensitive TEXT column, confirmed in V5__add_memory_notes.sql: `status TEXT NOT NULL DEFAULT 'ACTIVE'`), the supposedly-forgotten note's title keeps surfacing in search/RAG results indefinitely — a direct violation of the "forget this" deletion guarantee described in the class Javadoc.

**Fix:** Change the literal to uppercase 'DELETED' (and fix the identical bug in findSimilarNoteIds and the raw SQL in MemoryNoteService.java:371), or better, use UPPER(n.status) <> 'DELETED' / a status enum to avoid case-drift entirely.

<sub>Verifier: MemoryNoteRepository.java:35 filters `(n.status IS NULL OR n.status <> 'deleted')` using a lowercase literal. MemoryNoteEntity.status is a plain String column (MemoryNoteEntity.java:73-74, @Column with no converter/lowercasing), and MemoryForgetService.forget() sets it to the uppercase literal "DELETED" (line 88), consistent with other queries in the same repository/service that use uppercase status literals (e.g. `n.status = 'ACTIVE'` in findPendingReview, `"DELETED".equals(note.getStatus())` check at line 70). The DB column is TEXT (V5__add_memory_notes.sql:27) with no case-insensitive colla</sub>

## 15. [HIGH] memory-service — logic-error

**Location:** `apps/memory-service/src/main/java/org/jarvis/memory/obsidian/MemoryNoteService.java:216`

**Bug:** mergeDuplicate() (the default MERGE dedup-strategy path) never re-evaluates MemoryScope.isSensitiveScope / resolvePrivacy, so an incoming write whose scope is FINANCE/HEALTH gets silently folded into an existing duplicate without forcing local-only privacy or updating the note's scope — unlike update() and changeScope(), which both explicitly re-apply the guard.

**Failure scenario:** 1) Write note A with title/body X, scope=PROJECT, explicit privacy="public" (resolvePrivacy only forces local-only for FINANCE/HEALTH; PROJECT keeps the caller's requested privacy). 2) Later the same title/body X is resubmitted with scope=FINANCE (e.g. the owner reclassifies it as financial). Because dedup runs before scope/privacy resolution (writeWithOutcome computes contentHash and hits the dedup branch at lines 144-157, before `MemoryScope scope = ...` at line 161 is ever read), the duplicate hit routes to mergeDuplicate(existing, request), which only touches tags/linkedEntities/confidence/expiry/updatedAt — existing.scope stays PROJECT and existing.privacy stays "public". The note is now conceptually finance-sensitive but is neither scoped nor privacy-flagged as such, so MemoryService.privacyAllowed() will let it flow to an external/remote LLM provider, defeating the documented invariant at MemoryNoteService.java:258-267 ("privacy is always forced to local-only regardless of what the caller requested").

**Fix:** Before/inside mergeDuplicate, resolve the effective scope for the incoming request (falling back to existing.getScope() if request.getScope() is null) and re-apply the same guard used in update()/changeScope(): if isSensitiveScope(effectiveScope), set existing.setScope(effectiveScope) and existing.setPrivacy("local-only"); otherwise apply resolvePrivacy(effectiveScope, request.getPrivacy()) if the caller supplied a scope/privacy change. Alternatively, include scope in the dedup match key so a scope-reclassifying resubmission is never treated as a pure duplicate.

<sub>Verifier: Verified by reading the full body of mergeDuplicate (MemoryNoteService.java:216-245): it only touches tags, linkedEntities, confidence, expiresAt, updatedAt, and a frontmatter "dedup_merged_at" key. It never reads request.getScope()/request.getPrivacy(), never calls isSensitiveScope/resolvePrivacy, and never calls existing.setScope/setPrivacy. The dedup lookup key (repository method findFirstByContentHashAndStatusOrderByCreatedAtDesc, MemoryNoteRepository.java:93) is (contentHash, status) only, and contentHash (computeContentHash, line 282-283) hashes only normalized title+body — scope is not </sub>

## 16. [HIGH] planner-service — logic-error

**Location:** `apps/planner-service/src/main/java/org/jarvis/planner/service/RecurringTaskGenerator.java:55`

**Bug:** generateOccurrencesForDate's dedup guard only compares the requested date against the single lastGeneratedDate value, not whether that date was already materialized in a prior batch, so a manual generateNextOccurrences() call that jumps lastGeneratedDate past 'today' causes the next daily generation pass to re-create a duplicate occurrence for a date that was already generated.

**Failure scenario:** A WEEKLY (or INTERVAL) recurring template has lastGeneratedDate = null. A user calls POST /api/v1/planner/tasks/{id}/generate-next-occurrences?count=5 (RecurringTaskController -> RecurringTaskGenerator.generateNextOccurrences), which scans forward from the anchor date and materializes the next 5 due occurrences — e.g. today plus the next 4 matching weekdays — setting template.lastGeneratedDate to the LAST of those dates (several weeks in the future), while today's occurrence was already created as part of that same batch. The next time DailyPlanGenerator.generatePlan(userId, today) runs (daily cron/plan generation calling RecurringTaskGenerator.generateOccurrencesForDate), the check `date.equals(template.getLastGeneratedDate())` is false (lastGeneratedDate is a future date, not today), so it falls through to `isDue(template, today)`, which is true, and a second Task row is inserted for the same template/date — a duplicate occurrence (double-booked task) directly contradicting the class's documented invariant ('once per date, so re-running generation for the same date never double-books it').

**Fix:** Replace the scalar last-generated-date dedup with a real per-date existence check, e.g. query for an existing occurrence with `recurrenceSourceTaskId = template.getId()` and `dueDate` falling on that calendar day (or add a DB unique constraint on `(recurrence_source_task_id, occurrence_date)` and catch/ignore the constraint violation), instead of relying on comparing against a single `lastGeneratedDate` value which loses information about earlier dates already materialized in a batch.

<sub>Verifier: The dedup guard at line 55 of RecurringTaskGenerator.java is exactly as described: `if (date.equals(template.getLastGeneratedDate()) || !isDue(template, date)) { continue; }`. It only tracks a single scalar `lastGeneratedDate` per template, not a set/history of all dates already materialized.

Trace of the exploit path:
1. `generateNextOccurrences` (lines 78-112) is reachable via `POST /api/v1/planner/tasks/{id}/generate-next-occurrences` (RecurringTaskController line 60-66, confirmed by grep).
2. For a template with `lastGeneratedDate == null`, the cursor starts at `anchorDate(...)` (line 93-</sub>

## 17. [HIGH] agent-service — logic-error

**Location:** `apps/agent-service/src/main/java/org/jarvis/swarm/task/AgentTaskStatus.java:17`

**Bug:** isTerminal() omits FAILED even though FAILED is treated as a finished/terminal-ish status everywhere else (retention's FINISHED_STATUSES, SwarmCoordinator's isFinal()), causing AgentTaskService.cancel() to throw on any already-failed task.

**Failure scenario:** A client calls POST /api/v1/agents/tasks/{id}/cancel on a task that already finished with status FAILED (e.g. UI double-click after a failure notification, or a retried cancel request). In AgentTaskService.cancel() (queue/AgentTaskService.java:190-214), `task.status().isTerminal()` returns false for FAILED, so the method proceeds and calls `store.save(latest.cancelled(clock.instant()))`. AgentTask.cancelled() calls `to(CANCELLED, now)`, which checks `AgentTaskStatus.FAILED.canTransitionTo(CANCELLED)` — but the transition table only allows FAILED -> QUEUED (retry). This throws InvalidTransitionException, which SwarmExceptionHandler maps to HTTP 409, so a harmless/idempotent cancel-after-failure request fails with a conflict error instead of being a no-op. The developers were aware FAILED needed special handling elsewhere: SwarmCoordinator.isFinal() explicitly does `task.status().isTerminal() || task.status() == FAILED` to work around the same gap, but AgentTaskService.cancel() was never updated to do the same.

**Fix:** Either (a) update AgentTaskStatus.isTerminal() to also return true for FAILED (and separately track "can retry" as its own concept for the QUEUED-retry path), or (b) in AgentTaskService.cancel(), replace the isTerminal() checks with the same `isTerminal() || status == FAILED` predicate already used in SwarmCoordinator.isFinal(), returning false/no-op for an already-FAILED task instead of attempting FAILED->CANCELLED and letting InvalidTransitionException surface as a 409.

<sub>Verifier: Traced the entire call chain and it matches the report exactly:

1. AgentTaskStatus.isTerminal() (line 17-19) returns `this == COMPLETED || this == CANCELLED` — FAILED is excluded.
2. AgentTaskService.cancel() (queue/AgentTaskService.java:190-214) guards with `if (task.status().isTerminal()) return false;` (line 192) and again inside the lambda at line 208 `if (!latest.status().isTerminal())`. Neither guard catches FAILED, so for a FAILED task execution proceeds to `store.save(latest.cancelled(clock.instant()))` at line 209.
3. AgentTask.cancelled() (task/AgentTask.java:124-125) calls `to(Agen</sub>

## 18. [HIGH] media-service — race

**Location:** `apps/media-service/src/main/java/org/jarvis/media/job/MediaJobService.java:117`

**Bug:** The COMPLETED and FAILED transitions in runJob() unconditionally overwrite the job's status, unlike the CANCELLED transition which checks status().isTerminal() first, so a cancel request racing with job completion can flip an already-CANCELLED job back to COMPLETED.

**Failure scenario:** Worker thread in runJob() checks `token.isCancelled()` (false) right after `work.run(token)` returns, then before it executes `transition(id, j -> j.completed(...))` on line 117, a concurrent HTTP call to MediaJobService.cancel() runs: it sets the token, interrupts the (already-finished) future, and writes CANCELLED via `transition(id, latest -> latest.status().isTerminal() ? latest : latest.cancelled(...))` (line 100). The cancel() call returns `cancelled=true` to the client. Immediately after, the worker thread's pending `transition(id, j -> j.completed(...))` call (line 117) reads the just-written CANCELLED job and unconditionally builds a COMPLETED MediaJob from it (MediaJob.completed() does not check current status), overwriting the store back to COMPLETED. The client that just cancelled the job sees it later reported COMPLETED with a downloadable artifact, and the same bug applies symmetrically to the FAILED transition on line 125.

**Fix:** Add the same terminal-state guard used for cancel/finishCancelled to the completed and failed transitions, e.g. `transition(id, j -> j.status().isTerminal() ? j : j.completed(outcome.artifacts(), outcome.details(), clock.instant()));` and similarly for `failed`. Better yet, replace the read-then-save pattern in `transition()` with a proper atomic operation (e.g., `ConcurrentHashMap.compute`/`computeIfPresent` on the store, or a per-job lock/CAS with an expected-version check) so all terminal transitions are race-free rather than relying on each call site remembering to re-check `isTerminal()`.</fix_hint>
</invoke>


<sub>Verifier: The code at MediaJobService.java confirms the asymmetry the reporter describes. Line 100 (cancel()) and line 133 (finishCancelled()) both guard with `latest.status().isTerminal() ? latest : latest.cancelled(...)` before writing a terminal state, but the completed transition at line 117 (`transition(id, j -> j.completed(outcome.artifacts(), outcome.details(), clock.instant()))`) and the failed transition at line 125 apply the lambda unconditionally with no terminal-state check.

The store backing `transition()` (InMemoryMediaJobStore, confirmed by reading the file) is a plain `ConcurrentHashMap</sub>

## 19. [HIGH] media-service — logic-error

**Location:** `apps/media-service/src/main/java/org/jarvis/media/ffmpeg/FFmpegCommandBuilder.java:83`

**Bug:** mux() hardcodes `-metadata:s:a:1` for the added Russian audio track and `-metadata:s:s:0` for the added Russian subtitle track, assuming the original video has exactly one pre-existing audio stream and zero pre-existing subtitle streams.

**Failure scenario:** MuxService.submit() is called on a source video that has 0 or 2+ audio streams (e.g. a silent video, or a video with stereo + commentary tracks) or that already has an embedded subtitle track. Because `-map 0` copies every original stream first and the new Russian audio/subtitle streams are appended afterward, the added Russian audio track actually lands at stream index `(number of original audio streams)`, not fixed index 1, and the added subtitle lands at index `(number of original subtitle streams)`, not fixed index 0. The hardcoded `-metadata:s:a:1`/`-metadata:s:s:0` therefore tags the wrong (often an original, non-Russian) stream as `language=rus`/`title=Russian (neutral TTS)` while leaving the actual new Russian track unlabeled, causing players to present the wrong audio/subtitle track as the Russian dub.

**Fix:** Compute the type-relative output index dynamically instead of hardcoding it: track how many audio streams (and subtitle streams) input 0 contributes — ideally via an FFprobe call in MuxService before building the command — and use that count as the index for the new track's `-metadata:s:a:<count>` / `-metadata:s:s:<count>` (e.g., pass `originalAudioStreamCount` / `originalSubtitleStreamCount` into FFmpegCommandBuilder.mux() and build the specifier string from it, rather than the literal `1` and `0`).

<sub>Verifier: FFmpegCommandBuilder.mux() (lines 69-92) always emits `-map 0` first (copying every stream of the original file, in original order), then appends `-map audioInput:a` (new Russian audio) and `-map subInput:s` (new Russian subtitle) after that. FFmpeg's `-metadata:s:a:N` / `-metadata:s:s:N` specifiers are type-relative to the *output* stream ordering (the Nth audio/subtitle stream among the output's streams), not the input's absolute stream index. Since the new Russian audio/subtitle streams are appended after all original streams via `-map 0`, they only end up at `a:1`/`s:s:0` when the original</sub>

## 20. [HIGH] life-tracker — logic-error

**Location:** `apps/life-tracker/src/main/java/org/jarvis/lifetracker/util/CsvUtils.java:22`

**Bug:** parseCsv splits the whole document on raw newlines before doing quote-aware parsing, so a quoted field containing an embedded newline is torn into multiple bogus rows instead of being treated as one field.

**Failure scenario:** POST /api/v1/life/finance/import-csv-notifications with a CSV body containing a quoted multi-line bank push notification, e.g. `"Payment\nof 25,00 PLN\nLidl",HIGH` — a very plausible shape since real push notifications are multi-line and the endpoint's own javadoc (BankNotificationController.java:118-119) says rows are 'CSV-quoted so embedded commas / decimal commas survive.' CsvUtils.parseCsv (util/CsvUtils.java:22) calls `csv.split("\r\n|\r|\n")` before any quote tracking, so the line breaks inside the quoted field split the row into 3 fragments ('"Payment', 'of 25,00 PLN', 'Lidl",HIGH'). Each fragment is then parsed independently by parseLine as its own row: the notification text is corrupted/truncated across rows, the parser runs on garbage fragments (wrong amount/merchant/dedup key), and the 'HIGH' confidence marker ends up attached to the wrong fragment, producing wrong parsed transactions, spurious review-inbox drafts, or silently dropped/mis-imported rows. CsvUtilsTest only covers embedded newlines in escape() (export direction), never in parseCsv() (import direction), so this asymmetric round-trip bug is untested.

**Fix:** Rewrite parseCsv to do a single quote-aware scan over the whole document (tracking inQuotes state across characters, splitting into rows only on unquoted \r\n|\r|\n, and splitting fields only on unquoted commas) instead of pre-splitting on raw newlines with String.split before quote tracking begins. Add a CsvUtilsTest case for a quoted field containing an embedded newline to cover the import direction symmetrically with the existing escape() newline test.

<sub>Verifier: CsvUtils.parseCsv (line 22) does `csv.split("\r\n|\r|\n")` on the entire raw document before any quote-tracking logic runs. Verified empirically: parsing `"Payment\nof 25,00 PLN\nLidl",HIGH` yields three separate fragments (`"Payment`, `of 25,00 PLN`, `Lidl",HIGH`), each of which is then independently handed to parseLine as its own row — the quote-aware state machine in parseLine never sees the newlines because they're already gone by the time it runs. This is reachable in production: BankNotificationController.importCsvNotifications (lines 124-159) takes `body.get("csv")` straight from the re</sub>

## 21. [HIGH] life-tracker — logic-error

**Location:** `apps/life-tracker/src/main/java/org/jarvis/lifetracker/service/BankNotificationParser.java:115`

**Bug:** The bank-notification dedup key is bucketed by the current wall-clock hour at parse time (LocalDateTime.now()), not by anything derived from the transaction itself, so re-parsing/re-importing the exact same notification text after the hour rolls over produces a different dedup key and bypasses the (user_id, dedup_key) unique constraint entirely.

**Failure scenario:** A user imports a CSV export of bank notifications (or re-submits the same push notification after a client retry) at 23:58, then retries/re-imports the identical text at 00:02 the next hour. `dateBucket = LocalDateTime.now().format("yyyyMMddHH")` differs between the two calls (BankNotificationParser.java:115-119), so `dedupKey` differs even though amount/currency/merchant are identical, and expenseRepository.findByUserIdAndDedupKey / existsByUserIdAndDedupKey in BankNotificationController.java:64 and :144 finds no existing row — a second, duplicate Expense is stored, double-counting the transaction in every downstream finance summary, budget alert, and subscription detection despite the dedup unique index (uq_expense_user_dedup) being specifically designed to prevent exactly this.

<sub>Verifier: BankNotificationParser.parse() at line 115 computes `dateBucket = LocalDateTime.now().format("yyyyMMddHH")` — this is the wall-clock time at the moment `parse()` executes on the server, not anything derived from the notification text itself. Scanning the whole file confirms there is no date/time extraction logic anywhere in the parser (only amount, currency, type, card mask, and merchant are extracted from `raw`); `occurredAt` in the returned DTO (line 126) also uses `LocalDateTime.now()` rather than a parsed value. So the dedupKey (line 116-119) is `sha16(amount|currency|merchant|dateBucket)`</sub>

## 22. [HIGH] analytics-service — logic-error

**Location:** `apps/analytics-service/src/main/java/org/jarvis/analytics/service/MonthlyReportService.java:74`

**Bug:** refinedOverspendForecast() widens the anomaly-detection window to Math.max(MIN_ANOMALY_WINDOW, dayOfMonth) trailing days, but near the start of a month this trailing window spans into the PREVIOUS calendar month, while spentSoFar/dayOfMonth are strictly current-month figures — so a previous-month anomaly gets subtracted from the current month's total.

**Failure scenario:** On day 1 or 2 of a new month, dayOfMonth is 1 or 2, so Math.max(3, dayOfMonth) forces a 3-day trailing window that includes 1-2 days from the prior month. detectExpenseAnomalies(3, 2.0) can flag a high-spend day from the PRIOR month; its value is summed into excludedTotal and subtracted from spentSoFar (which only ever contains current-month expenses per budgetForecast()). Since spentSoFar is small/zero this early in the month, `adjustedSpent = Math.max(0.0, spentSoFar - excludedTotal)` collapses to 0, and `adjustedDailyRate = adjustedSpent / Math.max(1, dayOfMonth - anomalies.size())` (which can itself go negative and get clamped to 1) yields an artificially zeroed adjustedProjectedMonthEnd — a systematically wrong budget forecast every time last month ended with an anomalous high-spend day.

**Fix:** Either (a) filter `anomalies` in `refinedOverspendForecast` to only those with `day` within the current `YearMonth` before summing `excludedTotal`, or (b) add a month-boundary clamp in `TrendSeriesService.dailyExpenseTotals` (and/or a month-aware overload) so the anomaly-detection window never crosses into the previous month for this "current month to date" use case — e.g. `cutoff = today.minusDays(windowDays - 1).isBefore(month.atDay(1)) ? month.atDay(1) : cutoff`.

<sub>Verifier: Traced the full call chain:

1. `InsightService.budgetForecast()` (lines 103-131) computes `spentSoFar` by summing only expenses where `YearMonth.from(e.getOccurredAt()).equals(month)` — strictly current-month data — and sets `dayOfMonth = today.getDayOfMonth()`.

2. `MonthlyReportService.refinedOverspendForecast()` (line 74-75) calls `anomalyDetectionService.detectExpenseAnomalies(Math.max(MIN_ANOMALY_WINDOW, dayOfMonth), ANOMALY_K)`. On day 1 or 2 of a month, `dayOfMonth` is 1 or 2, so `Math.max(3, dayOfMonth)` forces `windowDays = 3`.

3. `AnomalyDetectionService.detectExpenseAnomalies` → `</sub>

## 23. [HIGH] smart-home-service — logic-error

**Location:** `apps/smart-home-service/src/main/java/org/jarvis/smarthome/service/IntentParser.java:139`

**Bug:** Numeric payload extraction for SET_TEMPERATURE/SET_BRIGHTNESS takes the FIRST number anywhere in the utterance instead of the number following the trigger word, so any earlier digit (e.g. a room/device number) is silently used as the value.

**Failure scenario:** User says "set thermostat 2 temperature to 22 degrees" (or RU equivalent). Tokens are [set, thermostat, 2, temperature, to, 22, degrees]; extractPayload runs NUMBER_PATTERN.matcher(normalizedUtterance).find() over the whole string and returns the FIRST match, "2", not "22". buildResult() sees a non-blank payload so it returns IntentMatchStatus.RESOLVED with CONFIDENCE_FULL (0.9) and payload="2". SmartHomeIntentService/executeAction downstream would then set the thermostat to 2°C (clamped/rejected only if outside 16-30 range, but here 2 is out of range and would be silently misreported as a confident, correct plan before ever reaching the range check) — the caller has no signal anything is wrong since confidence is full.

**Fix:** Mirror the SET_COLOR branch: compute a start index after the trigger word (e.g. `int afterTrigger = consumed.isEmpty() ? 0 : Collections.max(consumed) + 1;`), then search for the number pattern only among tokens (or in the substring of normalizedUtterance) from that position onward, e.g. iterate tokens from afterTrigger and match NUMBER_PATTERN against each token (or use `matcher.region(charOffsetOfToken(afterTrigger), normalizedUtterance.length())`), returning the first numeric token found after the trigger rather than the first number anywhere in the string. Also set consumedIndex correctly so the matched number token is excluded from buildDeviceQuery consistently with other payload types.

<sub>Verifier: In IntentParser.extractPayload (line 137-144), for NUMERIC_PAYLOAD_ACTIONS (SET_TEMPERATURE/SET_BRIGHTNESS), the code runs `NUMBER_PATTERN.matcher(normalizedUtterance).find()` against the entire raw normalized utterance string rather than scanning tokens starting after the matched trigger word's index. Because `find()` returns the leftmost match, any digit occurring before the trigger word (e.g. a room/device number) is returned instead of the actual value that follows the trigger.

I verified this empirically by compiling and running the real IntentParser class against the exact reported scen</sub>

## 24. [HIGH] smart-home-service — logic-error

**Location:** `apps/smart-home-service/src/main/java/org/jarvis/smarthome/controller/SmartHomeController.java:272`

**Bug:** Scene, group, and room bulk-action endpoints call the 3-arg SmartHomeService#executeAction overload which hardwires confirmed=false, so a scene/group/room step targeting a LOCK/DOOR/GARAGE device can never be executed via these endpoints — there is no confirm parameter exposed on activateScene, actOnGroup, or actOnRoom.

**Failure scenario:** An "evening lockup" scene contains a step {deviceId: front_door_lock, action: LOCK}. POST /scenes/evening-lockup/activate always calls executeAction(uid, deviceId, request) (line 272-273), which defaults confirmed=false (SmartHomeService.java lines 16-18). SafetyPolicy.requiresConfirmation returns true for LOCK, so the step always comes back with success=false/needsConfirmation=true — permanently, with no way for any caller to ever confirm it through the scene/group/room API. Same issue for actOnGroup (line 425) and actOnRoom (same helper). The feature silently never actually locks/unlocks/opens security-critical devices when driven through scenes, groups, or rooms.

**Fix:** Add a `confirm` field to SmartHomeScene.SceneStep (or a scene-level/group/room `@RequestParam boolean confirm`) and thread it through to the 4-arg executeAction(uid, deviceId, request, confirmed) call at activateScene (line 272-273) and applyActionToDevices (line 425), updating actOnGroup/actOnRoom signatures to accept the confirm flag as well.

<sub>Verifier: Verified directly in the code. SmartHomeService (lines 16-18) has a default 3-arg executeAction that always delegates with confirmed=false: `default SmartHomeActionResult executeAction(...) { return executeAction(userId, deviceId, request, false); }`. SmartHomeController.activateScene (line 272-273) calls exactly this 3-arg overload: `smartHomeService.executeAction(uid, step.deviceId(), new SmartHomeActionRequest(step.action(), step.payload()))`. Neither SmartHomeScene.SceneStep (model/SmartHomeScene.java:11, fields: deviceId, action, payload — no confirm field) nor the actOnGroup/actOnRoom en</sub>

## 25. [HIGH] sync-service — logic-error

**Location:** `libs/sync-protocol/src/main/java/org/jarvis/sync/SyncPayloadKind.java:15`

**Bug:** SyncPayloadKind's documented forward-compatibility guarantee (unrecognized kinds gracefully degrade to UNKNOWN) is not implemented: no @JsonEnumDefaultValue and no matching Jackson DeserializationFeature is configured, so Jackson throws InvalidFormatException on any kind string it doesn't recognize instead of falling back to UNKNOWN.

**Failure scenario:** A newer Android client (paired and holding a valid session key) sends a SyncEnvelope whose decrypted SyncPayload has a `kind` value added in a later protocol revision (e.g. a hypothetical LOCATION_PING) that this sync-service build hasn't shipped yet. In BlobInboxService.ingest (apps/sync-service/src/main/java/org/jarvis/syncservice/service/BlobInboxService.java lines 93-99), `mapper.readValue(plaintext, SyncPayload.class)` throws because the ObjectMapper (line 51) has no unknown-enum-value tolerance configured; the generic catch classifies this as Status.TAMPERED, returning HTTP 401 'payload JSON malformed' via SyncController.ingestBlob (apps/sync-service/src/main/java/org/jarvis/syncservice/controller/SyncController.java lines 59-75) and emitting a SYNC_BLOB_TAMPER_REJECTED audit event for legitimate, non-malicious traffic. The intended graceful path (kind resolves to UNKNOWN -> Status.UNSUPPORTED_KIND -> HTTP 422) is never reached for this scenario, so rolling upgrades where the phone ships a payload kind before the server does silently fail every sync of that kind instead of degrading gracefully as documented.

**Fix:** Add `@JsonEnumDefaultValue` to `UNKNOWN` in SyncPayloadKind.java and configure the production ObjectMapper in BlobInboxService.java (line 51) with `.configure(DeserializationFeature.READ_UNKNOWN_ENUM_VALUES_USING_DEFAULT_VALUE, true)` (or READ_UNKNOWN_ENUM_VALUES_AS_NULL plus the existing null-coalescing constructor logic), then add a BlobInboxService-level test asserting an unrecognized kind resolves to Status.UNSUPPORTED_KIND rather than TAMPERED.

<sub>Verifier: SyncPayloadKind.java (lines 3-16) documents a forward-compatibility guarantee that unrecognized kinds gracefully degrade to UNKNOWN, but no @JsonEnumDefaultValue annotation is present on the UNKNOWN constant. SyncPayload.java's constructor (line 33: `kind != null ? kind : SyncPayloadKind.UNKNOWN`) only substitutes UNKNOWN when Jackson supplies a null kind (e.g., field omitted) — it cannot run at all if Jackson throws while resolving an unrecognized enum string, because the exception happens inside Jackson's enum deserializer before the @JsonCreator constructor is invoked.

The production Objec</sub>

## 26. [HIGH] nlp-service — logic-error

**Location:** `apps/nlp-service/src/main/java/org/jarvis/nlp/service/impl/TextNormalizer.java:26`

**Bug:** Number-word-to-digit and filler-word replacement in TextNormalizer never actually fires because String.replaceAll's \b is ASCII-word-boundary-only (no UNICODE_CHARACTER_CLASS flag), so it never matches around pure-Cyrillic tokens.

**Failure scenario:** Java's \b (compiled without Pattern.UNICODE_CHARACTER_CLASS) only recognizes [a-zA-Z0-9_] as 'word' characters, so a boundary never occurs at the edges of a Cyrillic word like "пять" or "пожалуйста". Empirically verified: TextNormalizer.normalize("Поставь таймер на пятнадцать минут") returns "поставь таймер на пятнадцать минут" unchanged (number words and filler-word stripping are complete no-ops). This directly breaks EnhancedRuleBasedNlpService's EXPENSE pattern (line 67), which requires a literal digit group `(\d+)`: calling analyzeWithConfidence("потратил пять рублей на кофе", "ru") returns intent=UNKNOWN, confidence=0.2 instead of add_expense with amount=5/category=кофе, because the spoken number "пять" is never converted to "5". Any expense logged with a spelled-out Russian number instead of a numeral silently fails to be recognized.

**Fix:** Compile the replacement patterns with Pattern.UNICODE_CHARACTER_CLASS (e.g., Pattern.compile("\\b" + Pattern.quote(key) + "\\b", Pattern.UNICODE_CHARACTER_CLASS).matcher(t).replaceAll(val) instead of t.replaceAll(...)), or replace \b with explicit Cyrillic-aware lookarounds like (?<![\p{L}\d])word(?![\p{L}\d]), consistent with the RXF flag combo (CASE_INSENSITIVE | UNICODE_CASE | UNICODE_CHARACTER_CLASS) already used correctly in EnhancedRuleBasedNlpService's own patterns.

<sub>Verifier: TextNormalizer.normalize (line 26 and also lines 30-37) uses String.replaceAll("\\b" + Pattern.quote(word) + "\\b", ...), which compiles the pattern with default (ASCII-only) flags. Java's \b without Pattern.UNICODE_CHARACTER_CLASS defines "word" characters as [a-zA-Z0-9_]; Cyrillic letters are all \W. When both characters flanking a candidate boundary are non-word (e.g., start-of-string/space and a Cyrillic letter), no \b match occurs, so the number-word and filler-word replacement loops (lines 24-27, 36-38) as well as the unit-normalization lines (30-33) never fire on pure-Cyrillic tokens.

</sub>

## 27. [HIGH] nlp-service — logic-error

**Location:** `apps/nlp-service/src/main/java/org/jarvis/nlp/service/impl/DateTimeEntityExtractor.java:80`

**Bug:** Weekday detection uses plain substring containment on 4-7 letter Russian stems, causing common unrelated words to be misdetected as a weekday reference.

**Failure scenario:** extractDate() does `if (text.contains(entry.getKey())) return entry.getValue();` where entry keys include short stems like "сред" (meant to match среда/среду/средам = Wednesday) and "суббот". These stems are substrings of common, unrelated Russian words: "средство" (product/item, e.g. "моющее средство" = detergent) and "субботник" (volunteer cleanup day). Verified: EnhancedRuleBasedNlpService.analyzeWithConfidence("напомни купить моющее средство", "ru") returns add_reminder with entities={date=WEDNESDAY, text=купить моющее средство} even though the user never mentioned any day of the week. This causes reminders to be silently tagged/scheduled for the wrong date.

**Fix:** Replace substring `contains()` with word-boundary regex matching for each stem (e.g., compile `Pattern.compile("\\bсред[а-я]*\\b", RXF)` per stem, or build one alternation pattern with `\b(понедельник|вторник|сред[а-я]*|четверг|пятниц[а-я]*|суббот[а-я]*|воскресень[а-я]*)\b` and use `.find()`), so stems only match at word boundaries and not as substrings of unrelated words like "средство"/"субботник".

<sub>Verifier: The code at lines 79-83 of DateTimeEntityExtractor.java is exactly as reported:
```java
for (Map.Entry<String, String> entry : WEEKDAY_STEMS.entrySet()) {
    if (text.contains(entry.getKey())) {
        return entry.getValue();
    }
}
```
WEEKDAY_STEMS (built in buildWeekdayStems()) includes short stems "сред" (Wednesday) and "суббот" (Saturday) that are unanchored substrings — `String.contains` performs plain substring matching with no word-boundary check, unlike the other patterns in this file (TOMORROW, TODAY, etc.) which use `\b` regex boundaries.

Tracing the caller confirms the failure</sub>

## 28. [HIGH] orchestrator — non-idempotent-retry

**Location:** `apps/orchestrator/src/main/java/org/jarvis/orchestrator/service/impl/OrchestratorServiceImpl.java:744`

**Bug:** dispatchPcAction() unconditionally falls back to the direct host pc-control HTTP path whenever the API-Gateway/WebSocket path reports executionSucceeded=false, even when the desktop executor was found and actually attempted the action, risking the same non-idempotent action being executed twice.

**Failure scenario:** User says "mute"/"next track"/"undo". The connected desktop agent executes the action via WebSocket but the API Gateway response comes back with executionSucceeded=false (e.g. a transient ack/timeout glitch on the gateway side, or the desktop reports a soft error after already toggling state). dispatchPcAction (line 744-754) sees !executionSucceeded and calls tryDirectPcControl(...) which sends the identical action to the host pc-control bridge on the same physical machine. The action now runs twice: MUTE/UNMUTE flips back to the original state, PLAY_PAUSE toggles twice (no visible change reported as success), NEXT_TRACK skips two tracks, or CTRL+Z (undo) removes an extra undo step — the user is told the action succeeded but the actual system ends up in the wrong state.

**Fix:** In dispatchPcAction, only fall back to tryDirectPcControl when the WebSocket path genuinely could not reach/attempt execution (e.g. !dispatchResult.executorFound() || !dispatchResult.executionAttempted()), not merely when executionSucceeded is false; for the ack_timeout/soft-failure case (executorFound && executionAttempted but !executionSucceeded), surface the failure to the caller instead of retrying via a second, independent execution channel — or make the pc-control action idempotent by passing/checking the same requestId/correlationId so a duplicate direct-path call is a no-op.

<sub>Verifier: In OrchestratorServiceImpl.dispatchPcAction (lines 728-767), the fallback to tryDirectPcControl is gated solely on `!dispatchResult.executionSucceeded()` (line 744) — it does not check `executorFound` or `executionAttempted` first. Tracing the gateway side confirms the failure scenario is reachable, not hypothetical:

- PcControlWebSocketHandler.dispatchPcAction (apps/api-gateway/.../PcControlWebSocketHandler.java) delivers the PC_ACTION message to the connected desktop WebSocket client, then waits up to DEFAULT_ACK_TIMEOUT_MS=4000ms for an ACK (line 393). If the ACK does not arrive in time (`</sub>

## 29. [HIGH] orchestrator — degraded-partial-response

**Location:** `apps/orchestrator/src/main/java/org/jarvis/orchestrator/service/impl/OrchestratorServiceImpl.java:687`

**Bug:** handleSmartHomeAction() marks execution metadata as success after the smart-home action succeeds, but the subsequent best-effort NOTIFY dispatch (sendPcAction) can throw PcActionDispatchException, which is caught upstream and overwrites the returned response text with the generic ACK_ERROR phrase while executionSucceeded stays true.

**Failure scenario:** User says "turn on kitchen light". smartHomeClient.executeAction succeeds and rememberExecutionMetadata(ExecutionMetadata.success()) is set (line 685). sendPcAction("NOTIFY", ...) at line 687-689 then fails (both API-Gateway and direct pc-control unreachable), throwing PcActionDispatchException. executeIntent's catch block (lines 659-665) sees executionAttempted=true so it does NOT overwrite the (already-success) metadata, but it DOES overwrite the return value with phraseProvider.getPhrase(ACK_ERROR, lang). The result: IntentExecutionResult.executionSucceeded=true / executionFailed=false, yet responseText is an error phrase. VoiceLoopController.executeDirect() then uses result.executionFailed()==false to pick templates.executed(result.responseText()), so the voice assistant literally speaks the error phrase as a "success" utterance (VoiceSessionStatus.COMPLETED) even though the light really turned on — a materially wrong response to the user for a smart-home action that actually succeeded.

**Fix:** In handleSmartHomeAction(), wrap the best-effort NOTIFY dispatch in its own try/catch (or use a non-throwing notify variant) so a PcActionDispatchException from the desktop notification cannot escape and be caught by the generic RuntimeException handler in executeIntent(); log/swallow the notify failure and still return the smart-home success phrase built from `result` at lines 691-694. Alternatively, capture the correct response text before the sendPcAction(NOTIFY...) call and re-return it explicitly if only the notify step throws, instead of relying on the outer catch's generic ACK_ERROR fallback.

<sub>Verifier: Traced the full path and it matches the report exactly.

1. handleSmartHomeAction() (line 668-695): after smartHomeClient.executeAction() succeeds, line 685 calls rememberExecutionMetadata(ExecutionMetadata.success()) which sets executionAttempted=true, executionSucceeded=true. Then lines 687-689 call sendPcAction("NOTIFY", ...) as a best-effort desktop notification.

2. sendPcAction() (line 717-726) throws PcActionDispatchException (extends RuntimeException, line 128) whenever dispatchPcAction()'s result has executionSucceeded()==false.

3. dispatchPcAction() (line 728-767) tries the API Gate</sub>

## 30. [HIGH] llm-and-voice — race

**Location:** `apps/voice-gateway/src/main/java/org/jarvis/voicegateway/voiceloop/VoiceSessionController.java:88`

**Bug:** Barge-in /cancel does not stop or gate an in-flight /utterance dispatch, and the utterance handler unconditionally overwrites the CANCELLED status afterwards, so cancellation is silently undone and the underlying action still executes.

**Failure scenario:** Client POSTs /sessions/{id}/utterance, which starts intent resolution and then calls orchestratorClient.dispatch(...) (OrchestratorVoiceClient allows up to 30s, jarvis.voice.orchestrator-timeout-ms). While this is in flight, the user says 'stop'/barge-in and the client POSTs /sessions/{id}/cancel, which calls VoiceSessionRegistry.cancel() and sets status=CANCELLED. There is no shared cancellation flag or interrupt passed into the still-running utterance thread: it keeps running, and when orchestratorClient.dispatch() returns, VoiceSessionController.utterance() calls registry.update(sessionId, s -> { s.setStatus(reply.status()); ... }) (line 92-97) with no check of the current session status, silently reverting CANCELLED back to whatever the orchestrator returned. The side effect the user tried to cancel (e.g. a PC-control or smart-home action already dispatched to the orchestrator) still completes, and polling GET /sessions/{id} shows the command as succeeded instead of cancelled.

**Fix:** Before applying the post-dispatch update, re-check current status inside the same synchronized update (e.g., registry.update should accept a predicate/guard, or the controller should check registry.get(sessionId) status == CANCELLED and skip overwriting it if so). Additionally, thread a cancellation signal (e.g., a per-session CompletableFuture/AtomicBoolean or CancellationException) into the dispatch call so an in-flight orchestrator call can be aborted/ignored rather than only masking the local status after the fact.

<sub>Verifier: VoiceSessionController.utterance() (lines 87-97) makes a synchronous blocking call to OrchestratorVoiceClient.dispatch(), which uses RestTemplate with a read timeout of up to jarvis.voice.orchestrator-timeout-ms (default 30000ms). While this call is in flight on one request thread, a concurrent HTTP POST to /sessions/{id}/cancel runs on a different thread and calls VoiceSessionRegistry.cancel(), which sets status=CANCELLED (registry lines 74-85). When dispatch() eventually returns, utterance() unconditionally executes registry.update(sessionId, s -> { s.setStatus(reply.status()); ... }) (lines</sub>

## 31. [HIGH] llm-and-voice — swallowed-error

**Location:** `apps/llm-service/src/main/java/org/jarvis/llm/service/LlmService.java:175`

**Bug:** memoryClient.searchContext() is called with no try/catch, so any memory-service failure (down, timeout, 5xx) throws MemoryClientException straight out of processMessage() and fails the entire chat turn, even though RAG memory is documented as an optional/best-effort augmentation.

**Failure scenario:** memory-service pod is unreachable or times out. User sends a normal chat message with jarvis.llm.enabled and memory.enabled both true. LlmService.processMessage() reaches `memoryClient.searchContext(...)` (line 175-177) inside `if (memoryEnabled)` with no surrounding try/catch (unlike the ingestAsync call a few lines below, which IS wrapped in try/catch at line 239-251). MemoryClient.searchContext() throws MemoryClientException on any RuntimeException from the HTTP call. That exception propagates uncaught out of processMessage(), so the LLM chat call never even happens and the user gets a hard error instead of a normal (memory-less) assistant reply.

**Fix:** Wrap the memoryClient.searchContext(...) call (LlmService.java lines 175-177) in a try/catch for MemoryClient.MemoryClientException (or RuntimeException), logging a warning and falling back to an empty/degraded SearchContextResult (memoryContext = "") so the chat turn proceeds without memory context — mirroring the non-fatal handling already used for ingestAsync at lines 239-251.

<sub>Verifier: LlmService.processMessage() (lines 165-194) calls memoryClient.searchContext(...) at lines 175-177 inside `if (memoryEnabled)` with no try/catch. MemoryClient.searchContext() (MemoryClient.java lines 55-110) wraps its RestTemplate.exchange() call in a try/catch that re-throws ANY RuntimeException (connection refused, timeout via ResourceAccessException, 5xx via HttpServerErrorException, etc.) as an unchecked MemoryClient.MemoryClientException. Because processMessage() has no local catch for this, the exception propagates straight up through the method — the LLM chat call at line 221 is never r</sub>

## 32. [HIGH] desktop-javafx — blocking-ui-thread

**Location:** `apps/desktop-javafx/src/main/kotlin/org/jarvis/desktop/service/PcControlWebSocketClient.kt:203`

**Bug:** Every PC_ACTION handler body is wrapped in `uiDispatcher { ... }` (default `Platform.runLater`), but the action itself (SystemControlService.changeVolume/mediaControl/openApp/executeHotkey/lockScreen/etc.) runs `ProcessBuilder(...).start()` followed by a synchronous `process.waitFor()`/blocking read of stdout, so real OS command execution happens on the JavaFX Application Thread.

**Failure scenario:** The orchestrator sends a PC_ACTION over the PC Control WebSocket (e.g. VOLUME_UP, LOCK_SCREEN, or a SCENARIO composed of several chained commands). `handlePcAction` dispatches the whole synchronous block via `Platform.runLater`, so `SystemControlService.executeProcess` blocks the UI thread for the full duration of the external process (e.g. `xdotool`, `loginctl lock-session`, or a multi-step scenario chaining several `ProcessBuilder.start()+waitFor()` calls) — freezing the entire desktop shell (no repaint, no input) until the process(es) exit or hang.

**Fix:** Do not wrap the systemControl.* invocation in uiDispatcher/Platform.runLater. Execute the action on a background executor/thread (e.g., a dedicated single-thread ExecutorService or CompletableFuture.supplyAsync), and only marshal the resulting status update / sendAck callback back onto the FX thread via uiDispatcher after the process completes.

<sub>Verifier: In PcControlWebSocketClient.kt, handlePcAction() (lines 194-328) wraps the ENTIRE action-execution switch statement in `uiDispatcher { ... }` (line 203), whose production default (line 26) is `{ action -> Platform.runLater(action) }`. Test files override this default to a synchronous no-op (`uiDispatcher = { action -> action() }`) purely for testability — they do not fix or mitigate the production behavior.

Every branch of the `when (action.uppercase())` block (lines 216-306) calls into SystemControlService methods (changeVolume, setVolume, mute/unmute, mediaControl, openApp, openUrl, execute</sub>

## 33. [HIGH] android-app — coroutine-cancellation

**Location:** `apps/android-app/app/src/main/java/org/jarvis/android/sync/SyncWorker.kt:81`

**Bug:** The per-item sync loop in doWork() catches `Exception`, which also catches kotlinx.coroutines.CancellationException, so a WorkManager-initiated cancellation (constraints no longer met, timeout, OS reclaiming the worker) is swallowed instead of propagated.

**Failure scenario:** WorkManager cancels the running SyncWorker's coroutine (e.g. network constraint lost mid-run). Instead of the loop unwinding immediately, the in-flight network call throws a CancellationException that is caught by `catch (e: Exception)`, logged as an ordinary failure, recorded via dao.markFailed(...), and the `for (item in pending)` loop continues to the *next* item, performing further network I/O and DB writes after cancellation was requested. doWork() then returns Result.retry()/success() normally instead of letting the cancellation propagate, defeating WorkManager's ability to stop the worker promptly and violating structured-concurrency cancellation semantics.

**Fix:** Rethrow cancellation before the generic catch, e.g. `catch (e: CancellationException) { throw e } catch (e: Exception) { ... }` (or catch a narrower type like IOException), and optionally check `isStopped`/`coroutineContext.ensureActive()` at the top of each loop iteration so the worker exits promptly on WorkManager-initiated cancellation instead of continuing to the next pending item.

<sub>Verifier: Line 81's `catch (e: Exception)` in SyncWorker.kt does catch kotlinx.coroutines.CancellationException, since on the JVM that class is `java.util.concurrent.CancellationException`, which extends `IllegalStateException` -> `RuntimeException` -> `Exception`. Kotlin has no special-casing that makes `catch (e: Exception)` skip it, so this is a real Kotlin/coroutines footgun, and the project's own `~/.claude/rules/ecc/kotlin/coding-style.md` explicitly calls it out ("Never catch CancellationException — always rethrow it").

The reporter's exact mechanics are slightly off: `SyncClient.postBlob()` (ap</sub>

## 34. [HIGH] python-services — logic-error

**Location:** `apps/llm-server-py/app/main.py:267`

**Bug:** Streaming chat requests are never wrapped in the MAX_GENERATION_SECONDS timeout that protects the non-streaming path, despite the endpoint docstring claiming the timeout applies.

**Failure scenario:** POST /api/v1/llm/chat docstring (main.py:194-199) states 'Timeout: MAX_GENERATION_SECONDS' for the endpoint. The non-streaming branch enforces it via asyncio.wait_for at lines 222-233, but _handle_streaming_chat (line 267) and its generate_stream() (line 270) never apply any timeout around the token loop. A stuck or abnormally slow backend generation (e.g., llama.cpp hang, huge n_ctx growth) with stream=true will run indefinitely with no server-side cutoff, unlike the documented behavior, tying up the connection and any downstream proxy resources without bound.

**Fix:** Wrap the token consumption loop with a deadline check (e.g., compare `time.time() - start_time` against `config.MAX_GENERATION_SECONDS` each iteration and `break`/raise if exceeded), or run the blocking generator in an executor and drive it via `asyncio.wait_for` per chunk / with an overall `asyncio.wait_for` around an async wrapper that pulls from the executor-backed generator, so a stuck backend can't stall the stream (or the event loop) indefinitely.

<sub>Verifier: In apps/llm-server-py/app/main.py, the /api/v1/llm/chat endpoint docstring (lines 194-199) states "Timeout: MAX_GENERATION_SECONDS" applies to the endpoint as a whole. For the non-streaming branch (lines 214-244), this is enforced correctly via `asyncio.wait_for(loop.run_in_executor(...), timeout=config.MAX_GENERATION_SECONDS)`.

When `request.stream` is true and `config.ENABLE_STREAMING` is set (line 208), control instead goes to `_handle_streaming_chat` (line 267) which returns a `StreamingResponse` wrapping `generate_stream()` (line 270). Inside `generate_stream()`, the code does:

```
for </sub>

## 35. [HIGH] python-services — resource-leak

**Location:** `apps/llm-server-py/app/main.py:222`

**Bug:** asyncio.wait_for only abandons the awaiting coroutine on timeout; the underlying ThreadPoolExecutor worker keeps running the model call to completion, and with CHAT_WORKERS defaulting to 1, that worker thread is unavailable for all subsequent requests until it eventually finishes.

**Failure scenario:** config.py:42 sets CHAT_WORKERS default to 1, so `executor = ThreadPoolExecutor(max_workers=config.CHAT_WORKERS)` (main.py:56) has a single worker thread. If chat_handler.process_chat() (invoked via loop.run_in_executor at main.py:224-231) takes longer than MAX_GENERATION_SECONDS (e.g., a pathological prompt or backend stall), asyncio.wait_for raises TimeoutError and a 504 is returned to that caller, but Python's ThreadPoolExecutor cannot cancel an already-running task — the sole worker thread stays occupied running the original generation. Every subsequent /api/v1/llm/chat request then queues behind that thread indefinitely (or itself times out after MAX_GENERATION_SECONDS while still not actually running), effectively taking the whole chat endpoint down until the original hung call finally completes.

**Fix:** Add cooperative cancellation inside process_chat/model_loader (e.g., a threading.Event checked between token-generation steps, or run generation in a separate process via ProcessPoolExecutor/subprocess that can be hard-killed on timeout) so a timed-out request actually frees the worker; alternatively, track in-flight futures and reject new requests fast with 503 when the pool is saturated instead of silently queuing behind a stuck job, and consider raising CHAT_WORKERS or bounding queue depth with a semaphore.

<sub>Verifier: All the facts cited in the report check out in the code:

- config.py:42 — `CHAT_WORKERS: int = int(os.getenv("CHAT_WORKERS", "1"))` defaults to 1.
- main.py:56 — `executor = ThreadPoolExecutor(max_workers=config.CHAT_WORKERS)` creates a pool with a single worker thread by default.
- main.py:220-233 — the chat endpoint wraps `loop.run_in_executor(executor, lambda: chat_handler.process_chat(...))` in `asyncio.wait_for(..., timeout=config.MAX_GENERATION_SECONDS)`.
- chat_handler.py `process_chat` calls `model_loader.chat(...)` / `model_loader.generate(...)` synchronously with no internal timeout</sub>

## 36. [HIGH] python-services — concurrency

**Location:** `apps/embedding-service-py/app/main.py:179`

**Bug:** The async /embed and /embed/single endpoints call the CPU-bound SentenceTransformer encode path synchronously inline instead of offloading to a worker thread, blocking the asyncio event loop for the full inference duration.

**Failure scenario:** embed_batch() (main.py:158-196) and embed_single() (main.py:199-229) are `async def` handlers that directly call `embedder.embed_batch(...)` / `embedder.embed_single(...)` (main.py:179, 215), which in turn call `self.model.encode(...)` synchronously (embedder.py:109-113, 151-156) with no `run_in_executor`/thread offload anywhere in this service (contrast with llm-server-py, which explicitly uses a ThreadPoolExecutor for its blocking generation calls). Any request with a non-trivial batch or on CPU-only hardware blocks the single-threaded event loop for the whole encode duration, so concurrent requests — including GET /health — stall until it completes, defeating async concurrency and risking liveness-probe failures under load.

**Fix:** Wrap the blocking encode calls with `await run_in_executor` (e.g. `await asyncio.get_running_loop().run_in_executor(None, embedder.embed_batch, request.texts, request.input_type.value)`) or use `await anyio.to_thread.run_sync(...)`, mirroring the ThreadPoolExecutor pattern already used in llm-server-py; alternatively run uvicorn with multiple workers as a stopgap, though thread offload is the correct fix since embed_batch/embed_single are not declared `def` (which would get Starlette's automatic threadpool dispatch).

<sub>Verifier: main.py:158-229 defines embed_batch/embed_single as `async def` FastAPI handlers that call embedder.embed_batch()/embed_single() directly (line 179, 215) with no await, no run_in_executor, no anyio.to_thread offload. embedder.py:109-113 and 151-156 show these call self.model.encode(...) — a synchronous, CPU-bound SentenceTransformer call that holds the GIL during vectorized computation. Because the handlers are declared `async def`, FastAPI/Starlette does NOT auto-dispatch them to a worker thread (that auto-offload only happens for plain `def` route functions); the encode call runs directly on</sub>

## 37. [MEDIUM] api-gateway — race

**Location:** `apps/api-gateway/src/main/java/org/jarvis/apigateway/interceptor/RateLimitInterceptor.java:33`

**Bug:** Lazy initialization of the shared `rateLimiter` field is a non-atomic check-then-act on a non-volatile instance field of a singleton @Component, racing under concurrent first requests.

**Failure scenario:** Two threads hit preHandle() concurrently right after startup with rate-limit.enabled=true; both observe `rateLimiter == null`, both construct and assign a new RateLimiter (double-checked-locking style code without synchronization or volatile), so one of the two RateLimiter instances is silently discarded and its permits/state lost, and due to missing volatile a thread on another core may not even observe the assignment promptly — under load this can transiently double the enforced throughput or let extra requests through right around startup, undermining the rate limit that is supposed to protect the gateway.

**Fix:** Eagerly initialize `rateLimiter` in a `@PostConstruct` method (after `@Value` fields are injected) instead of lazily in `preHandle`, e.g. `@PostConstruct void init() { if (rateLimitEnabled) rateLimiter = RateLimiter.create(maxRequestsPerMinute / 60.0); }`, removing the runtime null-check race entirely. If lazy init must be kept, guard it with `synchronized` or use `AtomicReference<RateLimiter>` with `compareAndSet`/`updateAndGet`, and mark the field `volatile` at minimum.

<sub>Verifier: RateLimitInterceptor is a @Component with no scope annotation, so it is a singleton Spring bean, and its single instance's preHandle() method is invoked concurrently by many servlet container worker threads for concurrent HTTP requests. The `rateLimiter` field (line 23) is a plain, non-final, non-volatile instance field. Lines 33-38 perform a classic broken lazy-initialization / double-checked-locking pattern with no synchronization and no volatile keyword:

```java
if (rateLimiter == null) {
    double permitsPerSecond = maxRequestsPerMinute / 60.0;
    rateLimiter = RateLimiter.create(permit</sub>

## 38. [MEDIUM] memory-service — logic-error

**Location:** `apps/memory-service/src/main/java/org/jarvis/memory/obsidian/MemoryNoteService.java:371`

**Bug:** The raw JDBC pgvector semantic-search query used by searchUnified() has the same lowercase 'deleted' vs stored uppercase 'DELETED' status mismatch as the keyword path, so its "exclude deleted" clause is dead code (masked in practice only because forget() also nulls the embedding column).

**Failure scenario:** Same status literal bug as the searchByText finding, in the native SQL string built at MemoryNoteService.java:369-373 (`AND (status IS NULL OR status <> 'deleted')`). Currently this is largely inert because forget() sets embedding=null and the query also requires `embedding IS NOT NULL`, but the exclusion condition itself provides no real protection — e.g. if a future embedding backfill job (mentioned in MemoryNoteEntity's Javadoc as a Phase 12 follow-up) re-embeds notes without checking status first, forgotten notes would resurface via semantic search with no status guard actually blocking them.

**Fix:** Change the literal to uppercase (`status <> 'DELETED'`) or better, compare case-insensitively (`UPPER(status) <> 'DELETED'`) to match the actual persisted value set by MemoryForgetService.forget(); apply the same fix to the analogous JPQL query in MemoryNoteRepository.searchByText (line 35) and the native query at line 51, and consider a shared constant/enum for status values to prevent recurrence.

<sub>Verifier: MemoryForgetService.forget() (line 88) sets the persisted status via note.setStatus(\"DELETED\") — uppercase. The raw JDBC pgvector query in MemoryNoteService.searchUnified() (line 371) filters with `AND (status IS NULL OR status <> 'deleted')` — lowercase literal. Standard SQL string comparison is case-sensitive by default (absent a case-insensitive collation/citext), so for a soft-deleted row `status <> 'deleted'` evaluates to TRUE, meaning the OR-condition is satisfied and the row is NOT excluded by this predicate. The status guard is therefore dead code as written. It is currently masked i</sub>

## 39. [MEDIUM] memory-service — resource-leak

**Location:** `apps/memory-service/src/main/java/org/jarvis/memory/config/AsyncConfig.java:10`

**Bug:** @EnableAsync is configured with no custom Executor bean, so Spring falls back to SimpleAsyncTaskExecutor, which spawns a brand-new unbounded thread per invocation instead of using a pooled/bounded executor.

**Failure scenario:** MemoryService.ingestAsync (annotated @Async) is invoked once per fire-and-forget ingest call. Under a burst of concurrent ingest requests (e.g. a busy conversation session or a bulk replay), each call spins up a new unmanaged OS thread with no queueing or cap, risking thread-exhaustion / OOM under load; there is no other TaskExecutor/ThreadPoolTaskExecutor bean defined anywhere in the module (verified via repo-wide grep).

**Fix:** Add a bounded @Bean TaskExecutor (e.g. ThreadPoolTaskExecutor with corePoolSize/maxPoolSize/queueCapacity and a rejection policy such as CallerRunsPolicy) in AsyncConfig, implement AsyncConfigurer to return it from getAsyncExecutor(), and optionally add an AsyncUncaughtExceptionHandler; alternatively configure spring.task.execution.pool.* properties which Spring Boot auto-detects as the default "applicationTaskExecutor".

<sub>Verifier: AsyncConfig.java only has @Configuration + @EnableAsync with no @Bean TaskExecutor defined. Repo-wide grep across apps/memory-service/src/main/java for "TaskExecutor|Executor" returns nothing besides the @Async annotation site, and no application.yml/.properties define spring.task.execution pool settings either. MemoryService.ingestAsync (line 335) is @Async and is invoked as fire-and-forget per the class comment ("Async version of ingest for fire-and-forget use"). Per Spring's documented behavior (AsyncExecutionAspectSupport.getDefaultExecutor / ProxyAsyncConfiguration), when @EnableAsync is </sub>

## 40. [MEDIUM] agent-service — race

**Location:** `apps/agent-service/src/main/java/org/jarvis/swarm/queue/AgentTaskService.java:130`

**Bug:** Idempotency-key check-then-act in submit() is not atomic: two concurrent submits with the same user+idempotencyKey can both pass the 'not found' check and each create/run a brand-new task, defeating the idempotency guarantee the API contract promises.

**Failure scenario:** A caller (e.g. an API gateway or client) retries a POST /api/v1/agents/tasks request with the same idempotencyKey after a timeout, and the retry races the original request's worker thread. Both threads call `store.findByIdempotencyKey(userId, idempotencyKey)` (line 130) before either has saved a task, both get Optional.empty(), and both proceed to create a new taskId, submit it to the executor, and run it. For CODER this means duplicate patch proposals/applies; for TESTER with RUN_SHELL granted, the real allowlisted test command runs twice; for FINANCE/MEDIA roles, duplicate downstream side effects are proposed twice. The DB migration V2 explicitly notes the index is 'not a UNIQUE index' and that this race is out of scope, but no application-level locking (e.g. optimistic insert-and-catch-conflict) exists to close it, so the documented idempotency contract in CreateTaskRequest and AgentTaskService's javadoc is not actually honored under concurrent replay.

**Fix:** Add a per-(userId, idempotencyKey) in-process lock (e.g., a ConcurrentHashMap<String, Object> keyed on userId+idempotencyKey, or Guava Striped locks) around the check-then-act in submit(), and/or add a real UNIQUE constraint on (user_id, idempotency_key) in Postgres with an INSERT ... ON CONFLICT DO NOTHING / catch-DataIntegrityViolationException-then-re-read pattern so the store itself rejects true duplicates instead of relying solely on the non-unique lookup index.

<sub>Verifier: Lines 129-135 of AgentTaskService.submit() perform a classic check-then-act: `store.findByIdempotencyKey(userId, idempotencyKey)` is called, and only if empty does the method proceed to create a new task (line 141), save it (line 145), and submit it to the executor (line 158). There is no lock, no synchronized block, no distributed mutex, and no unique-constraint-with-conflict-handling around this sequence in AgentTaskService itself.

Checking all three AgentTaskStore implementations confirms none of them close the gap either:
- InMemoryAgentTaskStore#findByIdempotencyKey (lines 52-57) is a pl</sub>

## 41. [MEDIUM] media-service — logic-error

**Location:** `apps/media-service/src/main/java/org/jarvis/media/tts/DubAudioMergeCommandBuilder.java:60`

**Bug:** The `amix` filter is built without `normalize=0`, so ffmpeg's default per-input volume normalization (1/N) is applied even though the per-segment clips are time-delayed and never actually overlap, making the merged dub track progressively quieter as segment count grows.

**Failure scenario:** A transcript with, say, 20 dub segments is merged: each segment occupies a distinct, non-overlapping time slot via `adelay`, but ffmpeg's `amix` filter (normalize defaults to on) still divides every input's amplitude by the total input count (20) regardless of temporal overlap, because normalization is applied statically per input stream, not adaptively based on actual concurrent playback. The resulting `dub.ru.wav` track plays each segment at roughly 1/20th its original volume — far quieter than the source speech — defeating the purpose of the dub track and requiring the listener to raise volume drastically for a track that (per the class's own documentation) was designed to have no overlapping segments and thus should not need any attenuation at all.

**Fix:** Append `:normalize=0` to the amix filter string in DubAudioMergeCommandBuilder.buildFilterGraph (line 60-61), e.g. `amix=inputs=" + segments.size() + ":duration=longest:dropout_transition=0:normalize=0[aout]`, since segments are guaranteed non-overlapping and should not be attenuated by input count.

<sub>Verifier: The code at line 60-61 builds `amix=inputs=N:duration=longest:dropout_transition=0[aout]` with no `normalize=0` option, so ffmpeg's amix uses its default `normalize=true` behavior. I verified empirically (not just from docs) that ffmpeg's amix normalize is a static per-input scale of 1/N applied to every input stream regardless of whether other inputs are silent/non-overlapping at that point in time — it is NOT computed dynamically based on how many streams are actually concurrently audible.

Test with 2 non-overlapping (adelay-separated) copies of the same sine wave: original peak sample = 40</sub>

## 42. [MEDIUM] life-tracker — logic-error

**Location:** `apps/life-tracker/src/main/java/org/jarvis/lifetracker/service/BudgetAlertService.java:51`

**Bug:** overBudget requires `limit.signum() > 0`, so a budget configured with a zero limit is never classified as over-budget no matter how much is spent; it instead falls through to the overPace branch with an infinite ratio, producing a nonsensical 'Infinity%' message.

**Failure scenario:** User creates a Budget with limitAmount=0 (e.g. to flag a category that should have zero spend) and then logs any expense in that category. `overBudget = limit.signum() > 0 && ...` evaluates to false because signum(0) is 0, so BudgetAlertDTO.overBudget is false even though 100% of a zero budget was blown. Control falls into `overPace` (percentBudgetUsed = Double.POSITIVE_INFINITY from BudgetAlertService.java:85, which is > percentPeriodElapsed), and buildMessage (line 104-107) formats `%.0f%% used` on Double.POSITIVE_INFINITY, yielding the literal string '...ahead of budget pace: Infinity% used after N% of the month.' Any caller branching on the more specific `overBudget` flag (rather than the generic `alert`) mis-displays a zero-limit category as merely 'ahead of pace' instead of 'exceeded', and the rendered message is garbled.

**Fix:** Decouple the overBudget check from the signum guard: use `boolean overBudget = spent.compareTo(limit) > 0;` (BigDecimal.compareTo handles zero/negative limits correctly on its own), and keep the signum guard only inside percentBudgetUsed for the division-by-zero case. Optionally also add a `@DecimalMin("0.01")`/`@Positive` validation on BudgetDTO.limitAmount plus `@Valid` on the controller's `@RequestBody` to reject non-positive budget limits at the API boundary.

<sub>Verifier: The code at line 51 is exactly as described: `boolean overBudget = limit.signum() > 0 && spent.compareTo(limit) > 0;`. For a Budget with limitAmount == 0, signum() == 0, so overBudget is always false regardless of spent, even though any positive spend on a zero-limit budget is by definition over budget (spent.compareTo(BigDecimal.ZERO) > 0 alone would correctly detect this — the signum guard is unnecessary for this comparison and only makes sense as a divide-by-zero guard for percentBudgetUsed).

Tracing the fallthrough: percentBudgetUsed(limit, spent) at line 83-88 returns Double.POSITIVE_INF</sub>

## 43. [MEDIUM] analytics-service — clock-injection

**Location:** `apps/analytics-service/src/main/java/org/jarvis/analytics/service/AnalyticsService.java:260`

**Bug:** getCalendarStatistics() uses LocalDateTime.now() (system default timezone) instead of the injected Clock (configured as Clock.systemUTC() in AnalyticsApplication), unlike every other method in this class and sibling services.

**Failure scenario:** Every other date/time computation in the codebase (getSleepSummary, getOvertimeSummary, InsightService, TrendSeriesService, ChangeAnalysisService, HabitStreakService, MonthlyReportService, ConcreteAnswerService) consistently derives 'now'/'today' from the injected UTC Clock. getCalendarStatistics() alone calls LocalDateTime.now() with no zone, which resolves to the JVM host's default timezone. If the host is not running in UTC (e.g. Europe/*), a calendar event whose startTime is e.g. 22:00 UTC can be classified as 'past' hours before it actually occurs in UTC terms (or vice versa), because the 'now' boundary used for the upcoming/past split is offset from the UTC timeline the rest of the app (and presumably life-tracker's stored startTime values) uses. It also makes this one method untestable with a fixed Clock, unlike every other analytics computation.

**Fix:** Change line 260 to `LocalDateTime now = LocalDateTime.now(clock);` to match the pattern used everywhere else in the class and package; then update the test to build fixture events relative to `LocalDateTime.now(clock)` (the fixed clock already declared at line 43) instead of raw `LocalDateTime.now()`.

<sub>Verifier: apps/analytics-service/src/main/java/org/jarvis/analytics/service/AnalyticsService.java:260 reads `LocalDateTime now = LocalDateTime.now();` with no Clock argument, inside a class that has `private final Clock clock;` injected (constructor injection via @RequiredArgsConstructor) and wired in AnalyticsApplication as `Clock.systemUTC()`. Every sibling call site in the same class (lines 289, 291, 330, 351) and in every sibling service in the package (HabitStreakService, MonthlyReportService, TrendSeriesService, ChangeAnalysisService, InsightService, ConcreteAnswerService) consistently uses `Local</sub>

## 44. [MEDIUM] analytics-service — null-deref

**Location:** `apps/analytics-service/src/main/java/org/jarvis/analytics/controller/AnalyticsController.java:85`

**Bug:** getOverview()'s time-records aggregation unboxes TimeRecordDTO::getDurationSeconds (a Long) via mapToLong without filtering nulls, unlike AnalyticsService.getTimeStatistics() which explicitly filters `record.getDurationSeconds() != null` on the exact same data source.

**Failure scenario:** life-tracker returns a time record with a null durationSeconds (a realistic state for an in-progress/unstopped timer). `timeRecords.stream().mapToLong(TimeRecordDTO::getDurationSeconds).sum()` throws NullPointerException on auto-unboxing. This is caught by the generic `catch (RuntimeException e)` in that block, which discards the ENTIRE time-tracking section of the overview response and replaces it with a generic 'Unexpected error fetching time records' timeError — even though only one out of possibly many records was incomplete, and the sibling getTimeStatistics() endpoint would have returned correct totals by simply skipping that one record.

**Fix:** Filter nulls before summing, mirroring AnalyticsService.getTimeStatistics(): timeRecords.stream().filter(r -> r.getDurationSeconds() != null).mapToLong(TimeRecordDTO::getDurationSeconds).sum(); optionally also track/report the count of incomplete records instead of discarding the entire section on any single bad row.

<sub>Verifier: The core code defect is real and verified. In AnalyticsController.getOverview() (line 84-86):
    long totalDuration = timeRecords.stream()
            .mapToLong(TimeRecordDTO::getDurationSeconds)
            .sum();
TimeRecordDTO.durationSeconds is a `Long` (nullable box type, apps/analytics-service/.../dto/TimeRecordDTO.java line 24), backed by a life-tracker `time_record.duration_seconds BIGINT` column with no NOT NULL constraint (V1__initial_schema.sql) and no `@Column(nullable=false)` on the JPA entity (TimeRecord.java line 26). mapToLong on a null Long auto-unboxes and throws NPE, which</sub>

## 45. [MEDIUM] analytics-service — logic-error

**Location:** `apps/analytics-service/src/main/java/org/jarvis/analytics/service/ChangeAnalysisService.java:87`

**Bug:** workChange()'s verdict logic can never return "BETTER" and treats any weekly work-hour increase up to OVERTIME_JUMP_THRESHOLD_HOURS (3.0h) as "FLAT", inconsistent with the symmetric BETTER/WORSE/FLAT logic used by sleepChange() and spendChange() on the same FLAT_DELTA_THRESHOLD.

**Failure scenario:** A week where tracked work hours rise from 40.0 to 42.9 (delta=+2.9, well above the 0.05 FLAT_DELTA_THRESHOLD but below the 3.0 jump threshold) is classified "FLAT" and therefore excluded from whyWeekWentBad()'s regressions list and buildTomorrowImprovementAnswer()'s focus points — even though the same explanation text reports the +2.9h change to the user, and even though InsightService elsewhere explicitly treats sleep-loss-plus-overwork as a warning-worthy correlation (SLEEP_WORK_CORR). Real, moderate overwork increases are silently dropped from the 'what changed' / 'why did my week go bad' rule-based answers.

**Fix:** Make workChange symmetric like the other two: verdict = |delta| < FLAT_DELTA_THRESHOLD ? "FLAT" : delta > 0 ? "WORSE" : "BETTER"; if a distinct "big overtime jump" signal is still wanted, surface it as a separate flag/insight rather than folding it into (and overriding) the FLAT/WORSE/BETTER verdict used for regression filtering.

<sub>Verifier: ChangeAnalysisService.java lines 85-92 (workChange): delta = round(current - previous); verdict = |delta| < 0.05 ? "FLAT" : (delta > 3.0 ? "WORSE" : "FLAT"). For a delta of +2.9 (40.0 -> 42.9 hours), |delta| >= 0.05 so the first branch is skipped, and delta (2.9) is not > 3.0, so the else-branch yields "FLAT" — matching the reported scenario exactly. This is asymmetric versus sleepChange (line 79: delta < 0 ? "WORSE" : "BETTER") and spendChange (line 96: delta > 0 ? "WORSE" : "BETTER"), which are fully symmetric around FLAT_DELTA_THRESHOLD. Additionally, workChange can never produce "BETTER" f</sub>

## 46. [MEDIUM] nlp-service — logic-error

**Location:** `apps/nlp-service/src/main/java/org/jarvis/nlp/service/impl/DateTimeEntityExtractor.java:118`

**Bug:** applyDayPartQualifier only shifts hour+12 for "дня"/"вечера" but ignores "ночи" (night), so "в 12 ночи" (12 AM / midnight) is resolved to 12:00 (noon) instead of 00:00.

**Failure scenario:** For input containing "в 12 ночи" (at 12 midnight), CLOCK_HOUR_ONLY captures hour=12 and qualifier="ночи"; applyDayPartQualifier's isAfternoonOrEvening check only tests "дня".equals(q) || "вечера".equals(q), so hour stays 12 and the 12<12 branch never triggers for "ночи". Verified: EnhancedRuleBasedNlpService.analyzeWithConfidence("напомни встретиться в 12 ночи", "ru") returns entities={text=встретиться в 12 ночи, time=12:00}, i.e. a reminder meant for midnight (00:00) is stored as noon (12:00), a 12-hour scheduling error.

**Fix:** In applyDayPartQualifier, special-case the 12 o'clock boundary explicitly instead of only shifting <12 hours upward, e.g.: if ("ночи".equals(q) || "утра".equals(q)) { return hour == 12 ? 0 : hour; } if ("дня".equals(q) || "вечера".equals(q)) { return hour < 12 ? hour + 12 : hour; } return hour; This correctly maps "12 ночи"→00:00 and "12 утра" (if ever matched)→00:00, while "12 дня"/"12 вечера" remain 12:00/12:00 (well, "12 вечера" is unusual but stays consistent), and non-12 hours behave as before.

<sub>Verifier: Traced the actual regex and logic. CLOCK_HOUR_ONLY (line 33-34) matches "в 12 ночи" with group(1)="12", group(2)="ночи". This flows into formatClock(hour="12", minute="00", qualifier="ночи") → applyDayPartQualifier(12, "ночи"). At line 118: `boolean isAfternoonOrEvening = "дня".equals(q) || "вечера".equals(q);` only tests for "дня"/"вечера", explicitly omitting "ночи" (and "утра", though that's not part of this specific report). Since q="ночи" makes isAfternoonOrEvening=false, line 119's `isAfternoonOrEvening && hour < 12 ? hour + 12 : hour` returns hour unchanged (12), so the final formatted </sub>

## 47. [MEDIUM] orchestrator — circuit-breaker

**Location:** `apps/orchestrator/src/main/java/org/jarvis/orchestrator/service/impl/OrchestratorServiceImpl.java:1096`

**Bug:** The hand-rolled LLM circuit breaker (consecutiveFailures/circuitOpenUntil) resets the failure counter to 0 immediately after opening and gates re-entry with only a raw timestamp comparison (no half-open single-trial gate), unlike the shared SimpleCircuitBreaker used for the NLP/memory calls in the same class, so it takes a full new run of circuitBreakerFailureThreshold consecutive failures to reopen and allows unlimited concurrent requests through the instant the cooldown elapses.

**Failure scenario:** llm-service is down for an extended period. After 3 consecutive failures the circuit opens for circuitBreakerResetTimeoutSeconds (60s default) and consecutiveFailures is reset to 0 at line 1101. Once the 60s window elapses, every concurrent callLlm() invocation (line 962-966) simultaneously sees Instant.now().isBefore(openUntil)==false and is dispatched to the still-broken llm-service (no single half-open trial like SimpleCircuitBreaker.tryAcquire() provides), consuming multiple LLM executor threads/timeouts at once; and because the counter was reset to 0, it takes 3 more consecutive failures before the circuit reopens, so up to 2 more full-timeout (10s) requests per burst are sent to the dead service before protection kicks back in — defeating the purpose of the breaker during an extended outage.

**Fix:** Replace the hand-rolled consecutiveFailures/circuitOpenUntil fields in callLlm()/recordFailure() with the existing SimpleCircuitBreaker (already used for nlpCircuitBreaker): call llmCircuitBreaker.tryAcquire() before dispatching, llmCircuitBreaker.recordSuccess() on success, and llmCircuitBreaker.recordFailure(circuitBreakerFailureThreshold, Duration.ofSeconds(circuitBreakerResetTimeoutSeconds)) on failure, removing the manual AtomicInteger/AtomicReference pair entirely.

<sub>Verifier: The code at lines 954-1103 matches the reported defect exactly:

1. `callLlm()` gates entry only with `Instant openUntil = circuitOpenUntil.get(); if (Instant.now().isBefore(openUntil)) { ... return fallback; }` (lines 962-966). There is no CAS/single-trial flag guarding the moment `openUntil` elapses — every concurrent thread that reaches this check after the cooldown passes will proceed to call the (possibly still-dead) llm-service concurrently, each consuming an executor thread and a full `llmTimeoutSeconds` (default 10s) timeout before failing.

2. `recordFailure()` (lines 1094-1103) does:</sub>

## 48. [MEDIUM] orchestrator — timezone-misuse

**Location:** `apps/orchestrator/src/main/java/org/jarvis/orchestrator/service/impl/OrchestratorServiceImpl.java:596`

**Bug:** get_time/get_date intents use java.time.LocalTime.now() / LocalDate.now() with the JVM's default time zone instead of an explicit configured user/local zone, so in a container running with the default UTC zone the spoken time/date is wrong for the user.

**Failure scenario:** The orchestrator pod runs in a k3s container without an explicit TZ/user.timezone configuration (none found anywhere in the orchestrator module). User asks "который час" (get_time). LocalTime.now() at line 596 resolves against the JVM default zone (typically UTC in a container), so if the user is in UTC+3, Jarvis reports a time 3 hours behind the user's actual local time; get_date (line 603) can likewise report the wrong calendar date near local midnight.

**Fix:** Add an explicit ZoneId — either a configurable app property (e.g. `jarvis.timezone` bound via @Value, defaulting to a real user zone like Europe/Moscow) or, better, thread the user's timezone preference (mirroring the `prefs.getTimezone()` pattern already used in llm-service) through processText/processTextDetailed, then call `LocalTime.now(zoneId)` / `LocalDate.now(zoneId)` at lines 596/603 instead of the no-arg overloads. As a defense-in-depth measure, also set an explicit `TZ` env var (or JVM `-Duser.timezone`) in k8s/base/orchestrator/deployment.yaml so the container's implicit default is deterministic rather than relying on the base image's default.

<sub>Verifier: Lines 596 and 603 of OrchestratorServiceImpl.java use java.time.LocalTime.now() and java.time.LocalDate.now() with no ZoneId argument, which resolve against ZoneId.systemDefault() (the JVM default zone). Verification steps:

1. Confirmed no ZoneId/TimeZone/user.timezone reference exists anywhere in apps/orchestrator/src/main/java (grep returned zero hits), so there is no override of the JVM default anywhere in this module.
2. Checked the k8s deployment manifest (k8s/base/orchestrator/deployment.yaml) — no `TZ` env var or timezone-related configuration is set for the orchestrator container, so </sub>

## 49. [MEDIUM] pc-user-cloud — race

**Location:** `apps/user-profile/src/main/java/org/jarvis/userprofile/service/UserProfileProvisioningService.java:28`

**Bug:** Non-atomic check-then-act (existsByUserId then save) in profile auto-provisioning races under concurrent first access for the same new user, throwing an unhandled DataIntegrityViolationException.

**Failure scenario:** user_profile.user_id has a DB-level UNIQUE NOT NULL constraint (V1__create_user_profile_tables.sql line 6) matching the JPA @Column(unique = true) on UserProfile.userId. ensureProfileExists() (called from every read path: getPlanningContext/getGoals/getHabits/getPriorities via requireProfile()) checks userProfileRepository.existsByUserId(userId) then, if absent, saves a new UserProfile — with no unique-constraint-violation handling and no centralized @RestControllerAdvice in user-profile to translate it. Two concurrent requests for a brand-new user (e.g. two services fetching planning-context/goals simultaneously right after account creation) can both see existsByUserId()==false, both attempt INSERT, and the second commit throws a DataIntegrityViolationException that propagates as an uncaught 500 instead of returning the already-created profile.

**Fix:** Catch DataIntegrityViolationException around the save() call and, on catch, re-query (or return silently) since the profile now exists — effectively treating the unique violation as an idempotent success — or use a DB-level upsert (INSERT ... ON CONFLICT (user_id) DO NOTHING) via a native query/JPQL merge instead of exists-then-save.

<sub>Verifier: The code at lines 22-38 of UserProfileProvisioningService.ensureProfileExists is a textbook non-atomic check-then-act: it runs userProfileRepository.existsByUserId(userId) (line 28) and, if false, constructs and saves a new UserProfile (lines 32-37) in a separate statement, with no unique-constraint upsert, no INSERT ... ON CONFLICT, no pessimistic/optimistic locking, and no try/catch around save(). 

Verified supporting facts:
- V1__create_user_profile_tables.sql line 6 defines `user_id VARCHAR(255) UNIQUE NOT NULL`, so a second concurrent INSERT for the same userId will fail at the DB level </sub>

## 50. [MEDIUM] pc-user-cloud — race

**Location:** `apps/user-profile/src/main/java/org/jarvis/userprofile/service/UserPreferencesService.java:44`

**Bug:** Same TOCTOU pattern as the profile provisioning service: createDefaultPreferences() checks existsByUserId then inserts, racing under concurrent first-time GETs for the same user.

**Failure scenario:** user_preferences.user_id is UNIQUE NOT NULL (V3__add_personality_tables.sql line 6). UserPreferencesController.getPreferences() calls preferencesService.getPreferences(userId), and on empty Optional calls createDefaultPreferences(userId), which does repository.existsByUserId(userId) then repository.save(new UserPreferences(...)) with no re-check inside a transaction boundary that would prevent a second concurrent caller from also seeing 'not exists'. Two simultaneous GET /api/v1/profile/preferences/{userId} calls for a user with no preferences row yet can both attempt to insert; the second raises a DataIntegrityViolationException that is not caught anywhere (no @RestControllerAdvice in user-profile), returning a raw 500 instead of the already-created default preferences.

**Fix:** Rely on the UNIQUE constraint instead of exists-then-insert: attempt repository.save(...) directly (or use an upsert / INSERT ... ON CONFLICT DO NOTHING via a native query) inside a try/catch for DataIntegrityViolationException, and on catch re-fetch via repository.findByUserId(userId) and return that. Alternatively add a global @RestControllerAdvice mapping DataIntegrityViolationException to a safe response (e.g. re-query and return 200, or 409) so concurrent first-time GETs don't leak a raw 500.

<sub>Verifier: The code at UserPreferencesService.java:44-49 does exactly what is claimed: createDefaultPreferences(userId) calls repository.existsByUserId(userId) and, if false, proceeds to build a new entity and repository.save(entity) at line 61. The @Transactional annotation on the method only guarantees atomicity/rollback of the method's own statements — it does not add a lock or serialize concurrent transactions, so two concurrent transactions can both execute existsByUserId() and both see false (especially at READ COMMITTED, Postgres' default isolation level) before either commits its insert. The user</sub>

## 51. [MEDIUM] vision-security — path-traversal

**Location:** `apps/vision-security-service/src/main/java/org/jarvis/visionsecurity/service/EnrollmentStore.java:152`

**Bug:** sanitize(userId) only strips characters outside [a-zA-Z0-9._-], so a userId equal to "." or ".." passes through unchanged, letting userDirectory()/samplesDirectory()/profilePath() resolve one level above the intended 'users/<id>' directory.

**Failure scenario:** If a principal name (Authentication.getName()) is ever ".." (e.g. a service account, a federated IdP claim, or a future caller that passes userId from a request param), userDirectory("..") resolves to storage-root/users/.. = storage-root itself, so saveEnrollment/reset() end up creating/deleting files like storage-root/enrollment/profile.json outside the per-user 'users' namespace, and recreateDirectory()/deleteRecursively() would wipe that shared location instead of a user-specific one.

**Fix:** In sanitize(), after replacing disallowed chars, explicitly reject or remap the reserved segments: `if (cleaned.equals(".") || cleaned.equals("..") || cleaned.isBlank()) { throw new IllegalArgumentException("Invalid userId"); }`. Better: in userDirectory(), verify the resolved path stays under the `users` root via `Path.of(root,"users").resolve(sanitized).normalize()` and confirm `startsWith(usersRoot)` before use, throwing otherwise — this closes the same gap in both EnrollmentStore and the duplicated IncidentStore.sanitize().

<sub>Verifier: The regex in sanitize() — `userId.replaceAll("[^a-zA-Z0-9._-]", "_")` (line 152-153 of EnrollmentStore.java) — allows the characters `.`, `-`, and `_` to pass through unmodified. Because `.` is in the allow-list, a userId of "." or ".." consists entirely of allowed characters and is returned byte-for-byte unchanged. userDirectory() then builds `Path.of(root, "users", "..")`, which normalizes to `root` itself (one level above the intended per-user directory), and reset()/saveEnrollment() call deleteRecursively()/recreateDirectory() on that resolved path — this really would wipe the shared stora</sub>

## 52. [MEDIUM] desktop-javafx — race

**Location:** `apps/desktop-javafx/src/main/kotlin/org/jarvis/desktop/service/WakeWordDetector.kt:23`

**Bug:** `isRunning` is a plain (non-`@Volatile`) `var` that is written by `start()`/`stop()` on the caller thread and read in the tight polling loop of `processMicrophoneAudio()` on the dedicated `WakeWordDetectorThread`, unlike the correctly `@Volatile`-annotated `currentState` field in the same class.

**Failure scenario:** When `stop()` sets `isRunning = false` and closes `targetDataLine`, the audio thread has no happens-before guarantee it will observe the new value promptly (no volatile/synchronization), so `while (isRunning)` can keep looping and repeatedly hit exceptions from the closed `TargetDataLine.read()` in a tight retry (no sleep on that exception path), pegging a CPU core and/or leaving the daemon thread alive past the 1-second `join()` window in `stop()`, which returns without the capture thread actually having terminated.

**Fix:** Annotate `isRunning` with `@Volatile` (matching `currentState`), or replace it with an `AtomicBoolean`, so writes in `start()`/`stop()` are guaranteed visible to `processMicrophoneAudio()` on the audio thread; optionally also add a small sleep/backoff in the generic `catch (e: Exception)` block in `processMicrophoneAudio()` to avoid a tight spin if the loop does briefly miss the flag update.

<sub>Verifier: Line 23 declares `private var isRunning = false` as a plain (non-@Volatile) field, in contrast to `currentState` at line 27-28 which is explicitly `@Volatile`. `isRunning` is written on the caller thread in `start()` (line 90) and `stop()` (line 116), and read in the polling loop `while (isRunning)` (line 194) plus inside the catch block (line 227) on the separate `WakeWordDetectorThread` (created at line 178-184). There is no `synchronized` block, `Lock`, or other happens-before mechanism (e.g. `AtomicBoolean`) guarding this field, so under the JMM there is no visibility guarantee for the wri</sub>

## 53. [MEDIUM] android-app — timezone-bug

**Location:** `apps/android-app/app/src/main/java/org/jarvis/android/data/health/HealthConnectManager.kt:39`

**Bug:** `now.truncatedTo(ChronoUnit.DAYS)` truncates an Instant to the UTC day boundary, not the device's local day boundary, so 'steps today' is computed over the wrong 24h window for any non-UTC user.

**Failure scenario:** A user in Poland (UTC+1/+2, the market this whitelist targets) opens the Health tab at, say, 1:00 AM local time on a new day. `startOfToday` is still truncated to the *previous* UTC midnight (which is 1-2 hours in the local future relative to true local midnight), so `read()` either omits the steps taken since local midnight (because startOfToday hasn't rolled over yet in UTC terms) or, depending on the offset direction, silently includes several hours of the prior calendar day's steps as 'today'. The reported step count is wrong by design for the entire early-morning window every single day, not just at DST edges.

**Fix:** Replace `now.truncatedTo(ChronoUnit.DAYS)` with a zone-aware calculation, e.g. `val startOfToday = LocalDate.now(ZoneId.systemDefault()).atStartOfDay(ZoneId.systemDefault()).toInstant()`, so the step count window aligns with the device's actual local calendar day rather than the UTC day boundary.

<sub>Verifier: Instant.truncatedTo(ChronoUnit.DAYS) truncates at UTC epoch-day boundaries because java.time.Instant carries no timezone offset — this is documented JDK behavior, not a misreading. In HealthConnectManager.kt line 39, `startOfToday = now.truncatedTo(ChronoUnit.DAYS)` is used directly as the lower bound of TimeRangeFilter.between(startOfToday, now) at line 47 to fetch "today's" steps, with no ZoneId.systemDefault() conversion anywhere in the file, in HealthScreen.kt (the only caller), or in the test file (HealthSnapshotTest.kt only tests the plain data class, explicitly noting HealthConnectManag</sub>

## 54. [MEDIUM] android-app — compose-recomposition

**Location:** `apps/android-app/app/src/main/java/org/jarvis/android/ui/finance/ManualFinanceScreen.kt:66`

**Bug:** `dao.recent(20)` is called directly in the composable body (not `remember`-ed), so a new Flow instance is created on every recomposition; `LaunchedEffect(flow)` keys on that instance and therefore cancels and restarts the Room-backed collection on every recomposition instead of once.

**Failure scenario:** Typing a single character into the Amount/Currency/Category/Description fields on this screen triggers recomposition of the whole composable (state hoisted via `remember { mutableStateOf(...) }` at the top). Each recomposition calls `dao.recent(20)` again, producing a distinct Flow object; `LaunchedEffect(flow)` sees a 'new' key every keystroke, cancels the previous Room query subscription, and re-subscribes from scratch. On a large pending_items table this re-issues the underlying Room query dozens of times per second of typing, causing jank and needless DB load instead of collecting the flow once and reacting only to actual data changes.

**Fix:** Hoist the flow out of the recomposing path, e.g. `val recentFlow = remember(dao) { dao.recent(20) }` then `val recent by recentFlow.collectAsState(initial = emptyList())` (or keep the manual `LaunchedEffect` but key it on `dao` / `Unit` instead of on the freshly-created flow instance), so the Room subscription is created once per composition instead of once per keystroke.

<sub>Verifier: Line 66 `val recent by dao.recent(20).let { flow -> ... }` calls `dao.recent(20)` directly in the composable body, not inside a `remember` block. `PendingItemDao.recent()` (PendingItemDao.kt:19) is a Room `@Query` returning `Flow<List<PendingItem>>`; Room's generated implementation builds this via a fresh `flow { ... }`/`callbackFlow` builder on every invocation, which uses default identity equality (no `equals` override). Because none of `amount`, `currency`, `category`, `description` are hoisted out of `ManualFinanceScreen` into a child composable or a remembered/derived scope, they are read</sub>

## 55. [MEDIUM] python-services — logic-error

**Location:** `apps/llm-server-py/app/chat_handler.py:87`

**Bug:** History-trimming slice uses `other_msgs[-(config.MAX_HISTORY_LENGTH - len(system_msgs)):]`; when the system-message count exactly equals MAX_HISTORY_LENGTH, the computed negative index is 0, and Python's `list[-0:]` equals `list[0:]` (the whole list), so trimming silently does nothing.

**Failure scenario:** With MAX_HISTORY_LENGTH configured to N and a request whose messages contain exactly N system-role entries (e.g. MAX_HISTORY_LENGTH=1 with one system message, a realistic low-history-limit deployment), process_chat() at chat_handler.py:83-87 computes `-(N - N) == -0 == 0`, so `other_msgs[-0:]` returns all of other_msgs unmodified instead of trimming to `MAX_HISTORY_LENGTH - len(system_msgs)` items. The safety limit meant to bound conversation length silently fails, letting arbitrarily long histories reach the backend and risk exceeding the model's n_ctx (llama.cpp context overflow) or causing excessive generation latency.

**Fix:** Guard the zero case explicitly, e.g. `keep = config.MAX_HISTORY_LENGTH - len(system_msgs); messages = system_msgs + (other_msgs[-keep:] if keep > 0 else [])` (also consider clamping keep to >=0 and logging when system messages alone exceed MAX_HISTORY_LENGTH).

<sub>Verifier: chat_handler.py:83-87: the guard `if len(messages) > config.MAX_HISTORY_LENGTH` only requires total message count to exceed the limit; it does not prevent `len(system_msgs) == config.MAX_HISTORY_LENGTH`. When that equality holds (e.g. MAX_HISTORY_LENGTH=1 and one system message, plus any other messages), the slice index becomes `-(MAX_HISTORY_LENGTH - len(system_msgs)) == -0 == 0`, and `other_msgs[0:]` returns the entire list unchanged (verified by direct execution: 10 other messages in, 10 out, trimming silently no-ops). config.py's `validate()` (lines 108-138) never enforces a minimum on MAX</sub>

## 56. [LOW] llm-and-voice — logic-error

**Location:** `apps/voice-gateway/src/main/java/org/jarvis/voicegateway/service/TtsService.java:171`

**Bug:** @Cacheable key for TTS synthesis omits speakingRate and pitch, so calls to synthesize() with the same text/language/voice but a different rate or pitch return stale cached audio synthesized at the previous rate/pitch.

**Failure scenario:** `@Cacheable(value = "tts", key = "#text + '-' + #languageCode + '-' + #voiceName")` on `synthesize(text, languageCode, voiceName, speakingRate, pitch)` (line 171-174) does not include speakingRate/pitch in the cache key. If any caller invokes `ttsService.synthesize(...)` for the same text/language/voice but a different speakingRate (e.g. a future user-adjustable speech-rate setting, or Piper's configurable speed), the second call gets the byte[] cached from the first call's rate/pitch instead of freshly synthesized audio at the new rate — audio speed/pitch silently does not change despite the caller passing new values.

**Fix:** Include speakingRate and pitch in the cache key, e.g. key = "#text + '-' + #languageCode + '-' + #voiceName + '-' + #speakingRate + '-' + #pitch", or switch to a custom KeyGenerator/@Cacheable(key = "T(java.util.Objects).hash(#text,#languageCode,#voiceName,#speakingRate,#pitch)") so future callers that vary rate/pitch don't get stale cached audio.

<sub>Verifier: The code at line 171 genuinely has the described defect: `@Cacheable(value = "tts", key = "#text + '-' + #languageCode + '-' + #voiceName")` on `synthesize(String text, String languageCode, String voiceName, Double speakingRate, Double pitch)` (lines 171-174) omits speakingRate and pitch from the cache key while both parameters are forwarded into `synthesizeDetailed` -> `synthesizeWithGoogle`, where they directly affect `AudioConfig.setSpeakingRate/setPitch` (lines 254-255). Spring's SpEL-based cache key generation only uses what's in the `key` expression, so two calls differing only in rate/p</sub>
