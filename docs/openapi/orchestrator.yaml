openapi: 3.0.0
info:
  title: Orchestrator Service API
  version: 1.0.0
  description: |
    Deprecated as an authoritative voice integration reference.
    This OpenAPI file is incomplete and intentionally skeletal.
    For the real voice boundary, use /VOICE_ARCHITECTURE.md and
    /VOICE_LAYER_REALITY.md instead.
paths:
  /api/v1/orchestrator/execute:
    post:
      summary: Execute a processed command
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                intent:
                  type: string
                parameters:
                  type: object
      responses:
        '200':
          description: Command execution started
