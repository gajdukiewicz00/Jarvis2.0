apiVersion: apps/v1
kind: Deployment
metadata:
  name: planner-service
  namespace: jarvis-prod
spec:
  template:
    metadata:
      annotations:
        jarvis.io/internal-tls-slice-planner-api-gateway: planner-api-gateway-v1
    spec:
      containers:
        - name: planner-service
          env:
            - name: API_GATEWAY_URL
              value: https://api-gateway.jarvis-prod.svc.cluster.local:8443
            - name: JARVIS_JAVA_TRUSTSTORE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jarvis-internal-tls-planner-api-gateway
                  key: truststore-password
            - name: JAVA_TOOL_OPTIONS
              value: -Djavax.net.ssl.trustStore=/tls-api-gateway/jarvis-internal-truststore.jks -Djavax.net.ssl.trustStorePassword=$(JARVIS_JAVA_TRUSTSTORE_PASSWORD)
          volumeMounts:
            - name: internal-tls-planner-api-gateway
              mountPath: /tls-api-gateway
              readOnly: true
      volumes:
        - name: internal-tls-planner-api-gateway
          secret:
            secretName: jarvis-internal-tls-planner-api-gateway
            items:
              - key: truststore.jks
                path: jarvis-internal-truststore.jks
              - key: ca.crt
                path: ca.crt
